# Yonder 0.1.3 Native File Transfer Design

**状态：Final Design Baseline**  
**版本：0.1.3**  
**前置基线：Yonder 0.1.1 产品与协议基线、Yonder 0.1.2 Enterprise Authentication Design**  
**设计主题：活动远程终端会话内的原生单文件上传与下载**

---

## 1. 最终结论

Yonder 0.1.3 只增加一个简单、直接、实用的能力：

> 用户在已经建立的交互式远程终端会话中，通过 Yonder 本地控制快捷键上传或下载一个普通文件。两端只需要同一个 `yon` 二进制，不依赖 `rz`、`sz`、ZMODEM、SFTP、SCP、Shell 命令、临时命令入口或任何额外软件。

正式快捷键为：

| 操作 | 快捷键 | 语义 |
| --- | --- | --- |
| 结束当前会话 | `Ctrl+]` 后按 `.` | 保持既有语义 |
| 发送字面 `Ctrl+]` | `Ctrl+]` 后再按 `Ctrl+]` | 保持既有语义 |
| 上传一个文件 | `Ctrl+]` 后按 `u` | 主控端文件传到被控端 |
| 下载一个文件 | `Ctrl+]` 后按 `d` | 被控端文件传到主控端 |
| 显示本地控制帮助 | `Ctrl+]` 后按 `?` | 显示当前版本定义的全部本地快捷键 |

文件传输由 Yonder 原生协议完成：

- 不经过远端 PTY/ConPTY；
- 不依赖远端 Shell；
- 不解析 Shell 命令；
- 不把文件字节混入终端数据流；
- 不需要被控端现场人员执行任何额外操作；
- 不需要 relay 识别、缓存或保存文件；
- 完成、取消或普通失败后返回同一个远程终端会话。

0.1.3 不增加顶层 `yon upload`、`yon download`、`rz` 或 `sz` 命令，也不增加文件浏览、目录传输、同步、删除、移动、重命名、权限管理或远程文件管理能力。

---

## 2. 冻结原则

0.1.3 的设计遵守以下原则：

1. **功能简单**：只解决活动终端会话中的单文件上传和下载。
2. **使用直接**：不退出终端、不另开连接、不重新认证。
3. **Yonder 原生**：两端由同一个 `yon` 二进制直接完成文件读写和传输。
4. **终端透明**：普通终端输入输出继续保持原始语义，Yonder 不理解 Shell。
5. **职责分离**：终端字节、终端控制和文件传输使用不同协议语义。
6. **安全默认**：不覆盖已有目标，不暴露半成品，不提升权限。
7. **跨平台统一**：Linux、Windows、macOS 的用户操作和行为契约一致。
8. **失败局部化**：普通文件错误只结束本次文件操作，不结束远端 Shell。
9. **兼容旧版本**：旧端点不支持时明确拒绝文件操作，终端本身继续工作。
10. **扩展有纪律**：所有依附活动交互会话、由主控端本地触发且不应发送到远端 PTY 的能力，统一优先使用 `Ctrl+]` 加单字符的本地控制命名空间。

“统一优先使用”不意味着任何未来能力都必须强塞进快捷键。只有满足上述会话内、本地触发、终端相邻条件的能力才适用；配置、relay 管理、企业认证管理和非交互自动化不属于该命名空间。

---

## 3. 规范术语

- **主控端**：当前运行 `yon connect`、承载本地键盘与显示的 `yon`。
- **被控端**：当前运行 `yon host`、创建远端 PTY/ConPTY 与 Shell 的 `yon`。
- **活动终端会话**：已经完成目标解析、端到端认证和终端建立并处于 Active 状态的会话。
- **本地控制前缀**：字节 `0x1d`，即 `Ctrl+]`。
- **上传**：主控端读取本地源文件，被控端写入远端目标文件。
- **下载**：被控端读取远端源文件，主控端写入本地目标文件。
- **传输方**：读取源文件并发送字节的一端。
- **接收方**：接收字节并创建目标文件的一端。
- **本地基准目录**：主控端启动本次 `yon connect` 时一次性捕获的绝对工作目录。
- **远端基准目录**：被控端为本次终端会话启动 Shell 时一次性捕获的绝对初始工作目录。

两个基准目录在会话生命周期内不可变。后续 `cd`、进程级工作目录变化或 Shell 内身份变化都不修改它们；若目录被删除、移动或变得不可访问，相对路径操作明确失败，不猜测新位置。远端基准目录不是 Shell 后续执行 `cd` 后的当前目录。Yonder 不跟踪、猜测或解析 Shell 当前目录。

---

## 4. 对既有基线的继承

### 4.1 继承 0.1.1

0.1.3 保持：

- 原有连接码、目标解析、OPAQUE、直连与 relay 连接模型；
- 原有 terminal-data 与 terminal-control 职责；
- 原生 PTY/ConPTY 和 Shell 行为；
- 终端尺寸、退出码和关闭语义；
- `Ctrl+] .` 的本地脱离语义；
- `Ctrl+] Ctrl+]` 的字面前缀发送语义；
- 未识别本地选择器继续按原始字节透明转发；
- 非交互输入不解释本地控制前缀；
- 普通输入输出不因文件传输能力而被解释为 Shell 命令或文件协议。

### 4.2 继承 0.1.2

完整链路仍然是：

```text
普通 Resolve 或 Enterprise Resolve
→ 原有端到端认证
→ Terminal Active
→ 用户显式触发原生文件传输
→ Terminal Active
```

因此：

- 企业 relay 下必须先完成 Enterprise Resolve；
- 文件传输不得形成普通 Resolve 旁路；
- 文件传输不重新执行企业认证；
- 文件传输不创建新的企业身份；
- relay 不读取、不保存文件内容；
- 文件传输继承当前活动终端会话的连接、端点身份、权限和生命周期。

### 4.3 对既有非目标的修订

此前“文件传输”整体属于非目标。0.1.3 只作以下精确修订：

> Yonder 支持活动交互式终端会话内、由主控端显式触发的原生单文件上传和下载。

其余文件系统管理能力仍属于非目标。

---

## 5. 0.1.3 的明确范围

### 5.1 包含

- 上传一个普通文件；
- 下载一个普通文件；
- 空文件；
- 大文件流式传输；
- 本地和远端相对或绝对路径；
- 目标目录形式；
- 安全临时文件；
- 大小与 SHA-256 完整性验证；
- 不替换已有目标的最终提交；
- 用户取消与文件子流终止；
- 连接中断清理；
- 新旧端点能力协商；
- 六个正式发布 target 的统一行为。

### 5.2 不包含

- 一次传输多个文件；
- 目录或递归目录传输；
- 文件浏览和目录树；
- 文件同步、镜像或增量差分；
- 后台传输队列；
- 并行分块；
- 断点续传；
- 自动压缩或自动归档；
- 文件搜索；
- 删除、移动、重命名；
- 文件预览或编辑；
- 权限、所有权、ACL 和扩展属性管理；
- 符号链接本体传输；
- 特殊文件传输；
- Shell 当前工作目录跟踪；
- Glob、通配符、Shell 变量或 `~` 展开；
- relay 文件存储；
- 独立于活动终端会话的文件传输；
- `rz/sz`、ZMODEM、SFTP、SCP、rsync 或其他外部协议兼容；
- 脚本化文件传输 CLI；
- 用户自定义快捷键；
- 文件传输配置项。

这些是 0.1.3 的版本边界，不阻止未来版本通过独立设计扩展，但未来扩展不得重新解释本版本契约。

---

## 6. 本地控制命名空间

### 6.1 快捷键集合

```text
Ctrl+] .
Ctrl+] Ctrl+]
Ctrl+] u
Ctrl+] d
Ctrl+] ?
```

其中：

- `.`：结束当前会话；
- `Ctrl+]`：发送字面 `Ctrl+]`；
- `u`：进入上传交互；
- `d`：进入下载交互；
- `?`：显示本地控制帮助。

选择器固定使用小写 ASCII。大写 `U`、`D` 和其他字节在 0.1.3 中不是快捷键，继续按未识别选择器处理。

### 6.2 输入状态机

```text
PassThrough
  └─收到 Ctrl+]→ PrefixPending

PrefixPending
  ├─收到 .       → Detach
  ├─收到 Ctrl+]  → 向远端发送字面 Ctrl+]，回到 PassThrough
  ├─收到 u       → StartUpload
  ├─收到 d       → StartDownload
  ├─收到 ?       → ShowHelp，回到 PassThrough
  └─收到其他字节 → 向远端发送 Ctrl+] 和该字节，回到 PassThrough
```

要求：

- 前缀和选择器可以跨本地读取块；
- 读取块边界不得改变语义；
- 未识别选择器保持既有透明转发；
- 识别 `u` 或 `d` 后，输入适配器必须立即停止向远端组装该读取块；
- 同一读取块中位于选择器之后的字节属于新启动的本地文件操作，按原顺序交给本地路径输入状态机；
- 这些字节绝不能在文件操作结束后重新发送到远端 PTY；
- 若文件操作在消费这些字节前因对端不支持、会话关闭或本地初始化失败而终止，未消费余量必须丢弃并显示本地提示，不能静默转发；
- 余量上限不得超过现有终端输入块上限；
- `?` 完成帮助输出后，同一读取块余量恢复为普通终端输入；
- 不允许静默丢失或错误重定向已经读取的输入字节。

### 6.3 模态状态中的本地控制

上传提示、下载提示和实际传输期间，本地控制前缀仍由 Yonder 解释，不交给路径字段或远端 PTY：

- `Ctrl+] .`：取消当前文件操作并结束整个终端会话；
- `Ctrl+] ?`：显示本地控制帮助，然后返回当前模态状态；
- `Ctrl+] u` 或 `Ctrl+] d`：显示 `file transfer already active`，保持当前操作；
- `Ctrl+] Ctrl+]`：在路径提示中只有当当前字段允许该 Unicode 控制字符时才插入；0.1.3 路径字段不允许控制字符，因此显示 BEL 并忽略；在实际传输中显示 BEL 并忽略；
- 未识别选择器：在模态状态中显示 BEL 并忽略，不得转发到远端。

`Ctrl+C` 是模态操作的直接取消键；它不需要先输入本地控制前缀，也不发送到远端 PTY。

### 6.4 非交互输入

文件传输只在主控端标准输入为交互式终端时提供。

非交互输入中：

- `Ctrl+]` 不具有本地前缀语义；
- `u`、`d` 和 `?` 不具有本地操作语义；
- 所有输入字节继续原样发送；
- 不提供脚本化文件传输入口。

---

## 7. 用户交互

### 7.1 本地交互通道

文件传输交互只在主控端标准输入是终端，并且 stdout 或 stderr 至少一个是终端时可用。

- 优先使用与远端终端显示相同的终端设备；
- 若 stdout 不是终端但 stderr 是终端，本地提示和状态写入 stderr；
- 若没有可用终端输出，`u`、`d` 和 `?` 显示固定不可用错误并立即返回普通终端，不读取路径；
- 路径提示、帮助和状态摘要属于本地 UI，不进入 terminal-data，也不写入日志；
- 本地 UI 必须在输出前确保从远端应用的当前行另起一行，并在退出模态状态后恢复既有终端显示规则。

### 7.2 上传

用户按：

```text
Ctrl+] u
```

Yonder 临时进入本地路径提示：

```text
[yonder upload]
local source:
remote destination [remote session start directory]:
```

规则：

- `local source` 必填；
- 本地相对路径相对于本地基准目录；
- `remote destination` 留空时，目标为远端基准目录下的源文件基本名；
- 远端相对路径相对于远端基准目录；
- 远端绝对路径按被控端操作系统规则解释；
- 路径按字面解释，不进行 Shell 展开；
- 路径中空格是普通字符，不要求 Shell 引号；
- `Ctrl+C` 在提示阶段取消本次上传；
- 输入为空、超长或编码无效时重新提示；
- Yonder 不自动创建目标父目录。

目标解析：

- 若远端目标是已存在目录，最终文件名使用本地源文件基本名；
- 若远端目标不存在，将其视为完整目标文件路径；
- 若最终目标已经存在，拒绝上传；
- 若目标父目录不存在，拒绝上传。

### 7.3 下载

用户按：

```text
Ctrl+] d
```

Yonder 临时进入本地路径提示：

```text
[yonder download]
remote source:
local destination [local connect start directory]:
```

规则：

- `remote source` 必填；
- 远端相对路径相对于远端基准目录；
- 远端绝对路径按被控端操作系统规则解释；
- `local destination` 留空时，最终目标为本地基准目录下的远端源文件基本名；
- 本地相对路径相对于本地基准目录；
- 路径按字面解释，不进行 Shell 展开；
- `Ctrl+C` 在提示阶段取消；
- Yonder 不自动创建目标父目录。

目标解析：

- 若本地目标是已存在目录，最终文件名使用远端源文件基本名；
- 若本地目标不存在，将其视为完整目标文件路径；
- 若最终目标已经存在，拒绝下载；
- 若目标父目录不存在，拒绝下载。

### 7.4 路径提示期间的终端处理

路径提示必须避免远端程序输出破坏本地输入行，同时又不能停止读取 PTY 输出。

因此：

1. 主控端暂停把远端终端输出写到本地显示；
2. 主控端继续读取远端终端输出；
3. 输出暂存在每会话最多 **256 KiB** 的有界延迟缓冲；
4. 路径提示完成或取消后，先恢复终端模式，再按原顺序写出延迟输出；
5. 若缓冲达到上限，Yonder 必须取消尚未开始的文件操作、恢复终端并立即写出缓冲内容；
6. 缓冲溢出不得关闭远端终端；
7. 文件子流只有在路径提示全部完成后才允许打开。

该缓冲只用于短暂的路径提示，不用于文件传输数据阶段。

### 7.5 文件传输期间的终端处理

文件传输是同步、模态操作：

- 主控端暂停向远端 PTY 发送普通本地键盘输入；
- 主控端继续实时读取和显示远端终端输出，防止 PTY 阻塞；
- 文件字节只进入独立文件子流；
- 0.1.3 不做持续动态进度条或光标重绘；
- 开始时只输出一条状态行；
- 成功、取消或失败时只输出一条完成摘要；
- 不让远端控制序列与本地动态进度界面相互破坏。

传输期间本地输入规则：

- `Ctrl+C`：取消当前文件传输，返回原终端；
- `Ctrl+] .`：取消文件传输并结束整个会话；
- 其他 `Ctrl+]` 选择器按 6.3 的模态规则处理；
- 其他输入不发送到远端、不排队、不在传输结束后重放；
- 第一次收到其他输入时可以发出本地 BEL，并保持传输；
- 状态提示必须明确写明“传输期间终端输入暂停，Ctrl+C 取消”。

这种行为是显式模态行为，不属于普通终端字节丢失。

### 7.6 帮助

`Ctrl+] ?` 显示当前版本全部本地控制快捷键，并立即返回终端。

帮助：

- 不进入分页器；
- 不启动外部程序；
- 不修改远端终端状态；
- 以完整状态行输出，随后恢复普通输入。

---

## 8. 文件与路径语义

### 8.1 单文件

一次文件操作只传输一个文件。

同一活动终端会话同一时刻最多存在一个文件传输，不排队、不并行。

### 8.2 源文件

源路径必须先打开，再根据已打开句柄的元数据判断对象类型和大小；不得先按路径检查、再按路径重新打开。

打开后的最终对象必须是普通文件。

允许：

- 空文件；
- 普通文件；
- 路径经过符号链接或等价重解析后最终指向普通文件。

拒绝：

- 目录；
- 管道；
- socket；
- 字符设备；
- 块设备；
- Windows 设备对象；
- 其他特殊文件。

Yonder 传输打开后的普通文件内容，不传输链接关系。

### 8.3 目标文件

最终目标在提交前必须不存在。

0.1.3 永不覆盖：

- 普通文件；
- 目录；
- 符号链接；
- reparse point；
- 任何其他已存在对象。

不提供：

- 覆盖确认；
- 强制覆盖；
- 自动改名；
- 自动追加；
- 自动创建父目录。

### 8.4 默认文件名

协议中的“基本文件名”必须是一个不含目录语义的单一名称组件。

接收方在把对端提供的基本文件名用于已有目标目录时，必须按接收端操作系统规则重新验证：

- UTF-8 非空；
- 不得为 `.` 或 `..`；
- 路径解析结果必须恰好包含一个普通名称组件；
- 不得包含接收端认可的路径分隔符、根、盘符、UNC、设备前缀或其他路径前缀；
- 不得是接收端拒绝创建的保留名称；
- 验证和最终目录拼接必须发生在接收端；
- 验证失败返回 `InvalidFileName`，不得退化为对完整路径的解释。

因此，跨系统传输时，一个在发送端合法的文件名可能无法作为接收端默认文件名。用户可以重新发起操作并显式提供接收端合法的完整目标路径。

### 8.5 路径编码

协议路径使用 UTF-8：

- 不允许 NUL、C0/C1 控制字符或 DEL；
- 编码后最长 4096 字节；
- 空路径只在明确允许默认值的目标字段中有效；
- Unix 上无法转换为 UTF-8 的原生路径不属于 0.1.3 支持范围；
- Windows 路径必须能在 Unicode 与本机路径之间无损转换；
- 主控端不得使用本机路径规则规范化远端路径；
- 远端路径只由被控端解释；
- 本地路径只由主控端解释。

### 8.6 元数据

0.1.3 只传输文件字节和用于确定默认目标名的基本文件名。

不保留：

- 所有者和组；
- Unix mode；
- ACL；
- 扩展属性；
- alternate data streams；
- 创建、修改和访问时间；
- 稀疏布局；
- 文件系统压缩属性；
- Finder 或其他平台元数据。

目标文件由接收端当前进程、目录、umask 和操作系统默认规则创建。

### 8.7 权限边界

- 被控端文件操作使用运行 `yon host` 的同一操作系统身份；
- 主控端文件操作使用运行 `yon connect` 的本地用户身份；
- Yonder 不提权、不切换用户、不绕过 ACL；
- 文件传输不建立额外授权模型；
- 文件传输能力不扩大运行 `yon` 的操作系统身份权限边界；
- 远端 Shell 内后续通过 `sudo`、`su` 或其他机制取得的临时身份不会传递给文件传输；
- 文件传输也不继承前台程序自己的沙箱、受限命令集或应用级授权；
- 本版本以产品基线中的“当前用户正常交互式 Shell”为前提；若未来支持受限 Shell 或自定义终端程序，必须重新设计是否启用文件传输及其授权边界。

---

## 9. 协议架构

### 9.1 独立应用协议

新增：

```text
/yonder/file-transfer/1.0.0
```

并在 `yonder-core` 中新增：

```text
wire::file_transfer
FILE_TRANSFER_PROTOCOL
```

每次上传或下载使用一条新的、有序、双向应用子流。

不新增：

- relay 文件协议；
- 独立文件连接码；
- 第二次 Resolve；
- 第二次 OPAQUE；
- 第二条终端会话；
- terminal-data 文件帧；
- terminal-control 文件帧。

### 9.2 会话绑定

文件子流必须：

- 与当前活动终端使用同一底层端点连接；
- 绑定同一个连接标识和已认证对端；
- 绑定当前终端会话；
- 只在 Terminal Active 后接受；
- 在终端会话关闭时立即取消；
- 不允许附着到另一终端会话；
- 不允许脱离活动终端独立存在。

被控端不得只按 PeerId 接受文件请求，必须按活动连接和当前会话定位上下文。

### 9.3 能力发现与旧版本

0.1.3 不修改现有 TerminalHello、TerminalReady 或 terminal-control 1.0.0。

每个活动连接维护三态能力缓存：`Unknown`、`Supported`、`Unsupported`。

首次触发上传或下载时：

1. 若状态为 `Unknown`，主控端先尝试协商 `/yonder/file-transfer/1.0.0`；
2. 协商成功后立即无消息关闭该探测子流，并缓存 `Supported`；
3. 被控端把“首帧前正常 EOF”视为无副作用的能力探测，不创建传输状态、不输出错误；
4. 只有明确的协议不支持结果才缓存 `Unsupported`；超时、连接错误和临时 I/O 失败不得伪装成不支持，也不得永久缓存；
5. 协商不支持时缓存 `Unsupported`，显示固定错误且不进入路径提示；
6. 状态为 `Supported` 时才进入路径提示，并在提示完成后打开真正的传输子流；
7. 状态为 `Unsupported` 时后续快捷键直接显示固定错误，不重复探测。

终端始终保持 Active；不支持不属于连接失败，也不得回退到 Shell 命令、ZMODEM 或其他协议。底层连接重建后能力缓存失效并重新从 `Unknown` 开始。

---

## 10. Wire 协议

### 10.1 帧与编码

每条 wire 消息采用统一帧：

```text
1 byte  tag
4 bytes payload_length, unsigned big-endian
N bytes payload
```

规则：

- tag、字段顺序和数值含义由 1.0.0 固定；
- `payload_length` 不得超过该 tag 的固定上限；
- 接收方必须先验证长度，再分配或读取 payload；
- 除 `Data` 外，所有 payload 均小于 8 KiB；
- `Data` payload 长度为 1 至 65536；
- EOF 只能出现在完整消息边界；
- 未知 tag、长度不符、字段越界和尾随字节均为协议错误；
- 所有整数使用无符号大端编码；
- 字符串使用显式 `u16` 字节长度加 UTF-8 字节；
- 不使用 JSON、CBOR、文本行或对端提供的自由文本错误；
- 解码不得按对端宣告的文件大小分配内存。

1.0.0 固定 tag：

| Tag | 消息 |
| --- | --- |
| `0x01` | `UploadOpen` |
| `0x02` | `DownloadOpen` |
| `0x03` | `DownloadOffer` |
| `0x04` | `Ready` |
| `0x05` | `Data` |
| `0x06` | `Finish` |
| `0x07` | `Committed` |
| `0x08` | `Cancel` |
| `0x09` | `Error` |

### 10.2 固定边界

| 项目 | 上限或类型 |
| --- | --- |
| 路径 | 4096 UTF-8 字节 |
| 基本文件名 | 1024 UTF-8 字节 |
| 文件大小 | `u64` |
| 单个数据块 | 1 至 65536 字节 |
| 完整性摘要 | SHA-256，32 字节 |
| 同会话活动传输 | 1 |
| 路径提示延迟输出 | 256 KiB |
| 内存模型 | 流式、有界，不按文件大小分配 |

各 tag 的 payload 长度固定为：

| 消息 | payload 长度 |
| --- | --- |
| `UploadOpen` | 12 至 5132 字节，且由字段长度精确决定 |
| `DownloadOpen` | 3 至 4098 字节，且由字段长度精确决定 |
| `DownloadOffer` | 11 至 1034 字节，且由字段长度精确决定 |
| `Ready` | 0 |
| `Data` | 1 至 65536 |
| `Finish` | 40 |
| `Committed` | 0 |
| `Cancel` | 0 |
| `Error` | 2 |

`UploadOpen` 的目标路径允许 0 长度以表达默认远端目标目录；其他路径和基本文件名字段必须非空。空文件不发送数据块，但仍执行 Finish、摘要验证和提交。

### 10.3 消息载荷

- `UploadOpen` (`0x01`)
  - `u16 destination_len`；
  - `destination_len` 字节目标路径；
  - `u16 file_name_len`；
  - `file_name_len` 字节源文件基本名；
  - `u64 declared_size`。
- `DownloadOpen` (`0x02`)
  - `u16 source_len`；
  - `source_len` 字节远端源路径。
- `DownloadOffer` (`0x03`)
  - `u16 file_name_len`；
  - `file_name_len` 字节源文件基本名；
  - `u64 declared_size`。
- `Ready` (`0x04`)：空 payload。
- `Data` (`0x05`)：1 至 65536 个文件字节。
- `Finish` (`0x06`)
  - `u64 actual_size`；
  - 32 字节 SHA-256。
- `Committed` (`0x07`)：空 payload。
- `Cancel` (`0x08`)：空 payload。
- `Error` (`0x09`)
  - `u16 error_code`。

字符串长度、整体 payload 长度与各字段必须完全相符，不允许尾随字节。一条文件子流只承载一次传输，因此不需要额外传输 ID。

方向规则：

| 操作 | 主控端可发送 | 被控端可发送 |
| --- | --- | --- |
| 上传 | `UploadOpen`、`Data`、`Finish`、`Cancel`、`Error` | `Ready`、`Committed`、`Error` |
| 下载 | `DownloadOpen`、`Ready`、`Committed`、`Cancel`、`Error` | `DownloadOffer`、`Data`、`Finish`、`Error` |

任何不符合当前操作方向和状态的消息都是协议错误。

### 10.4 结构化错误码

1.0.0 固定错误码：

| 数值 | 错误 |
| --- | --- |
| 1 | `Busy` |
| 2 | `InvalidRequest` |
| 3 | `InvalidPathEncoding` |
| 4 | `InvalidFileName` |
| 5 | `PathTooLong` |
| 6 | `SourceNotFound` |
| 7 | `SourceNotRegularFile` |
| 8 | `DestinationExists` |
| 9 | `DestinationParentNotFound` |
| 10 | `DestinationNotDirectory` |
| 11 | `PermissionDenied` |
| 12 | `NoSpace` |
| 13 | `FileTooLargeForPlatform` |
| 14 | `ReadFailed` |
| 15 | `WriteFailed` |
| 16 | `SizeMismatch` |
| 17 | `DigestMismatch` |
| 18 | `SourceChanged` |
| 19 | `CommitFailed` |
| 20 | `Cancelled` |
| 21 | `SessionClosing` |
| 22 | `Unsupported` |

数值 `0` 和未定义数值均为协议错误。CLI 只根据错误码输出固定、脱敏的本地消息。对端不得控制最终错误文案。

### 10.5 终止规则

- `Committed` 是唯一成功终态；发送后双方关闭该文件子流；
- 任一方可以在非终态发送 `Error`，发送后停止文件 I/O 并关闭子流；
- 主控端因用户取消发送 `Cancel`；接收方停止 I/O、删除未提交临时文件，并尽力回复 `Error(Cancelled)` 后关闭；
- 收到 `Cancel`、`Error` 或对端 EOF 后不得继续发送 `Data`、`Finish` 或 `Committed`；
- 首帧前正常 EOF 只用于 9.3 的能力探测；真正传输子流在首帧前 EOF 视为操作失败；
- 非法 tag、长度、顺序或状态转换只终止当前文件子流；只要底层连接和终端协议仍有效，远端终端保持 Active；
- 文件协议错误不得污染 terminal-control 状态，也不得让对端自由文本进入本地显示。

---

## 11. 上传状态机

### 11.1 主控端

```text
TerminalActive
→ EnsureFileTransferCapability
→ UploadPrompt
→ ValidateLocalSource
→ OpenFileStream
→ SendUploadOpen
→ AwaitReady
→ SendData
→ SendFinish
→ AwaitCommitted
→ TerminalActive
```

取消和普通失败返回 `TerminalActive`；连接丢失除外。

### 11.2 被控端

```text
TerminalActive
→ ReceiveUploadOpen
→ ResolveDestination
→ ValidateDestination
→ CreatePrivateTemporaryFile
→ SendReady
→ ReceiveData
→ ReceiveFinish
→ VerifySizeAndDigest
→ CommitNoReplace
→ SendCommitted
→ TerminalActive
```

失败时：

- 关闭文件子流；
- 尽力删除临时文件；
- 不创建或修改最终目标；
- 保持原终端会话。

---

## 12. 下载状态机

### 12.1 主控端

```text
TerminalActive
→ EnsureFileTransferCapability
→ DownloadPrompt
→ OpenFileStream
→ SendDownloadOpen
→ ReceiveDownloadOffer
→ ResolveLocalDestination
→ ValidateDestination
→ CreatePrivateTemporaryFile
→ SendReady
→ ReceiveData
→ ReceiveFinish
→ VerifySizeAndDigest
→ CommitNoReplace
→ SendCommitted
→ TerminalActive
```

### 12.2 被控端

```text
TerminalActive
→ ReceiveDownloadOpen
→ ResolveAndOpenSource
→ ValidateRegularFile
→ SendDownloadOffer
→ AwaitReady
→ SendData
→ SendFinish
→ AwaitCommitted
→ TerminalActive
```

被控端只有收到 `Committed` 才将下载视为成功。

---

## 13. 安全写入与最终提交

接收方必须：

1. 在最终目标的同一目录创建不可预测名称的私有临时文件；
2. 使用排他创建，禁止跟随已有临时路径；
3. 流式写入文件字节；
4. 同步计算接收字节数和 SHA-256；
5. 验证 Finish 中的字节数和摘要；
6. 完成应用与运行库层面的文件 flush，把已写数据交给操作系统；
7. 使用不替换已有目标的原子或等价安全提交；
8. 提交成功后才发送 `Committed`。

要求：

- 最终目标在提交前被其他进程创建时必须失败；
- 不允许先删除目标再重命名；
- 不允许直接截断或覆盖最终路径；
- 若文件系统无法提供安全 no-replace 提交，本次操作失败；
- 失败、取消、断线和进程退出时尽力删除临时文件；
- 临时文件不包含凭证或协议秘密；
- 普通日志不得记录临时文件名。

平台可以使用不同底层机制，但外部行为必须一致。例如可以使用支持 no-replace 的 rename、排他链接加清理或等价系统 API；不得为了共享实现而降低安全保证。

0.1.3 的成功语义不承诺突然掉电后的持久化落盘，也不要求对文件和父目录执行跨平台 `fsync` 等价操作。它保证在正常操作系统运行和 API 成功返回的前提下，最终路径只出现完整、已验证的文件；电源故障和存储设备故障属于文件系统与操作系统持久性边界。

---

## 14. 完整性与源文件变化

传输方和接收方同时计算 SHA-256。

接收方只有在以下条件全部满足时提交：

- 接收字节数等于宣告文件大小；
- Finish 中的实际发送字节数等于宣告文件大小；
- 接收方摘要等于 Finish 摘要；
- 文件写入与 flush 成功。

传输方必须：

- 打开源文件后从已打开句柄读取初始大小和可用修改标识；
- 只发送该初始大小范围内的字节；
- EOF 早于宣告大小时失败；
- 数据读取完成后、发送 `Finish` 之前，重新检查同一已打开句柄的文件大小和可用修改标识；
- 明确检测到源文件变化时发送 `Error(SourceChanged)` 而不是 `Finish`；
- 接收方收到 `SourceChanged` 后删除临时文件，不得提交。

0.1.3 不提供文件系统快照。若平台无法检测保持大小和时间戳不变的原地修改，Yonder 不承诺源文件对应单一文件系统时刻；但接收结果必须与传输方实际发送字节的 SHA-256 完全一致。

---

## 15. 资源、并发与超时

### 15.1 有界内存

- 数据块最多 64 KiB；
- 文件读写、网络读写和摘要计算全部流式执行；
- 不允许按完整文件大小分配内存；
- 路径和协议消息具有固定上限；
- 路径提示延迟输出固定为 256 KiB；
- 状态信息不得随时间无限增长。

### 15.2 终端公平性

文件传输不得让 terminal-data、terminal-control 或会话关闭控制无限期饥饿：

- 每处理一个有界数据块后必须把执行权交还异步调度器；
- 终端输出、终端尺寸、取消和关闭事件必须持续被轮询；
- 文件写入背压不得停止读取终端输出；
- 文件流不能持有阻止终端控制任务推进的全局锁；
- 实现不承诺在带宽饱和时终端具有固定延迟，但必须保证控制路径有界推进。

### 15.3 单传输

每个活动终端会话最多一个文件操作。

若已有文件操作处于提示、协商或传输阶段，新的 `Ctrl+] u` 或 `Ctrl+] d` 不启动第二个操作，显示：

```text
file transfer already active
```

不排队、不自动取消现有操作。

### 15.4 超时

- 子流打开、请求、Ready、Finish、Committed 等控制交换具有有界超时；
- 数据阶段没有总时长上限；
- 数据阶段具有无进展超时；
- 超时后取消、清理并返回终端；
- 精确时长属于固定实现常量，不提供配置。

### 15.5 文件大小

协议使用 `u64` 表示文件大小，不设额外产品级上限。

实际限制来自：

- 源和目标文件系统；
- 可用空间；
- 操作系统文件 API；
- 当前用户权限；
- 平台能够表达的文件大小；
- `u64` 协议边界。

---

## 16. 取消、断线与会话结束

### 16.1 提示阶段取消

本地 `Ctrl+C`：

- 取消路径提示；
- 不打开文件子流；
- 不向远端 PTY 发送 `Ctrl+C`；
- 恢复终端并写出延迟远端输出。

### 16.2 传输阶段取消

本地 `Ctrl+C`：

- 只取消当前文件操作；
- 不向远端 PTY 发送 `Ctrl+C`；
- 尽力发送 Cancel；
- 接收方删除临时文件；
- 恢复活动终端。

### 16.3 主动脱离

`Ctrl+] .`：

- 取消正在进行的文件操作；
- 尽力清理双方文件状态；
- 按既有语义结束整个终端会话；
- 不返回远端终端。

### 16.4 连接中断

- 当前文件操作立即失败；
- 未提交目标不得出现；
- 临时文件尽力删除；
- 0.1.3 不恢复、不续传；
- 终端会话按既有中断规则结束。

### 16.5 Shell 退出

远端 Shell 或 PTY 结束时：

- 文件操作取消；
- 未提交目标不得出现；
- 文件子流关闭；
- 终端退出码继续按原有协议处理。

---

## 17. 终端与文件传输隔离

### 17.1 数据隔离

文件字节只进入 file-transfer 子流，不得进入：

- terminal-data；
- terminal-control；
- PTY/ConPTY；
- Shell stdin/stdout；
- 本地终端显示；
- 日志。

### 17.2 生命周期隔离

路径错误、权限不足、目标存在、空间不足、摘要错误、源文件变化和用户取消等普通文件错误不得结束远端 Shell。

只有底层连接丢失、会话关闭或协议安全状态无法维持时，终端才按既有规则结束。

### 17.3 输入隔离

- 路径提示和传输期间普通本地输入不发送到远端；
- 快捷键触发前已经交给终端发送路径的字节可以完成发送；
- 快捷键选择器后同一读取块余量交给本地文件操作输入，不得在操作结束后发往远端；
- 传输阶段用户新输入不保留、不重放；
- 路径文本只属于本地主控 UI。

---

## 18. 安全、隐私与日志

### 18.1 授权边界

文件传输只对当前已认证活动终端开放。

它不增加文件级授权模型，因为主控用户已经获得同一操作系统身份下的交互式 Shell。文件读写继续受对应操作系统身份、文件权限和 ACL 约束。

### 18.2 禁止远端主动推送

被控端不得自行向主控端写文件。

所有上传和下载必须由主控端用户通过本地快捷键显式触发。下载虽然由被控端发送字节，也必须先收到主控端的 DownloadOpen。

### 18.3 路径安全

- 不执行路径；
- 不经过 Shell；
- 不展开变量；
- 不解释通配符；
- 不自动创建父目录；
- 不覆盖已有对象；
- 不把远端路径按本地规则重写；
- 临时文件安全排他创建；
- 最终提交必须 no-replace；
- 特殊文件不得作为源。

### 18.4 日志

普通日志可以记录：

- 本地请求 ID；
- 上传或下载方向；
- 协议阶段；
- 结果类别；
- 总字节数；
- 耗时。

不得记录：

- 本地或远端完整路径；
- 文件名；
- 文件内容；
- 数据块；
- SHA-256；
- 临时文件名；
- OPAQUE 状态；
- PeerId、locator；
- 企业身份数据。

### 18.5 relay 可见性

relay 不能读取文件内容、路径、文件名、摘要和协议明文，但仍可能观察连接时间、传输规模和时序等网络元数据。0.1.3 不承诺隐藏流量元数据。

---

## 19. 配置原则

0.1.3 不增加文件传输配置。

没有：

- 功能开关；
- 默认上传目录；
- 默认下载目录；
- 最大文件大小配置；
- 覆盖策略配置；
- 快捷键配置；
- 并发配置；
- 超时配置；
- 摘要算法配置；
- relay 文件配置。

固定行为用于保持功能简单、部署零配置、跨平台一致和测试可穷尽。

---

## 20. 版本与 relay 兼容

### 20.1 0.1.3 主控端连接旧被控端

- 远程终端正常工作；
- 文件快捷键触发协议协商；
- 对端不支持时显示固定错误；
- 终端保持 Active；
- 不执行任何降级；
- 路径不发送到远端 Shell。

### 20.2 旧主控端连接 0.1.3 被控端

- 终端行为保持旧版本语义；
- 旧主控端没有文件快捷键；
- 被控端不会主动开启文件流。

### 20.3 relay

普通 relay 和企业 relay：

- 不理解 file-transfer 消息；
- 不增加文件配置；
- 不缓存或保存文件；
- 只承载既有端到端连接中的应用子流。

### 20.4 禁止回退

不得回退到：

- ZMODEM；
- `rz/sz`；
- Shell `cat`；
- Base64；
- SCP/SFTP；
- relay 上传；
- 临时 HTTP 服务；
- 任何猜测性替代方案。

---

## 21. 跨平台契约

正式支持：

- Linux x86_64；
- Linux arm64；
- Windows x86_64；
- Windows arm64；
- macOS Intel；
- macOS Apple Silicon。

所有平台必须提供相同的：

- 快捷键；
- 单文件语义；
- 相对路径基准；
- 默认目标目录；
- 目标不存在要求；
- 临时写入；
- no-replace 提交；
- SHA-256 验证；
- 取消；
- 断线清理；
- 错误分类；
- 新旧版本兼容。

平台差异只允许存在于：

- 路径解析；
- 普通文件判断；
- 临时文件创建；
- no-replace 提交；
- flush；
- 控制台 raw/cooked 模式切换。

不得以“跨平台统一”为理由降低某个平台的安全语义。

---

## 22. 实现分层

### 22.1 `yonder-core`

新增：

- `wire::file_transfer`；
- `FILE_TRANSFER_PROTOCOL`；
- 消息编解码；
- 结构化错误码；
- 路径和帧边界；
- 协议状态机测试；
- 模糊测试。

### 22.2 `yon` 主控端

新增：

- 本地控制动作枚举；
- `Ctrl+]` 选择器扩展；
- 本地路径提示；
- 256 KiB 远端输出延迟缓冲；
- 上传发送端；
- 下载接收端；
- 模态输入控制；
- 取消和状态摘要；
- 本地临时文件及提交。

### 22.3 `yon` 被控端

新增：

- 活动终端会话上的文件子流处理；
- 同连接和同会话绑定；
- 上传接收端；
- 下载发送端；
- 远端路径解析；
- 远端临时文件与提交；
- 单会话传输互斥。

### 22.4 `yonder-net`

复用或最小扩展既有应用子流能力，不承载文件语义。

### 22.5 `yon-relay`

不增加文件协议解析、存储、缓存、配置、状态机或敏感日志。

---

## 23. 必测矩阵

### 23.1 本地控制状态机

覆盖：

- 所有前缀与选择器读取块切分；
- `Ctrl+] .`；
- `Ctrl+] Ctrl+]`；
- `Ctrl+] u`；
- `Ctrl+] d`；
- `Ctrl+] ?`；
- 大写和未知选择器透明转发；
- 选择器后余量进入本地文件操作且绝不重放到远端；
- 模态提示和传输中的 `Ctrl+] .`、`Ctrl+] ?`、重复 `u/d` 与未知选择器；
- EOF 时孤立前缀；
- 非交互完全透明；
- Windows 与 Unix 输入适配一致。

### 23.2 路径提示

覆盖：

- 正常输入；
- 空必填路径；
- 默认目标路径；
- 空格和 Unicode；
- 路径超长；
- `Ctrl+C`；
- 远端输出延迟与顺序恢复；
- 256 KiB 上限；
- 缓冲达到上限时取消提示但终端继续工作；
- 能力未知时先探测，旧端点不支持时不读取路径；
- 探测子流首帧前 EOF 无副作用；
- 提示结束前不打开真正的传输子流。

### 23.3 Wire 协议

覆盖：

- 所有消息往返；
- 未知标签；
- 错误长度；
- 非 UTF-8、控制字符路径和非法默认文件名；
- 尾随字节；
- 每个 tag 的最小、最大和精确 payload 长度；
- 0 长和超长 Data；
- 长度累加溢出；
- Ready 前 Data；
- 重复 Finish；
- Finish 前 Committed；
- 大小不一致；
- 摘要不一致；
- 非法方向、非法状态转换；
- 各阶段 Cancel、Error、首帧前 EOF 和成功终止；
- 模糊输入不得 panic 或无界分配。

### 23.4 文件语义

覆盖：

- 0、1、65535、65536、65537 字节；
- 大文件流式传输；
- 超过 4 GiB 文件；
- 本地和远端相对路径；
- 本地和远端绝对路径；
- 默认目标目录；
- 目标为现有目录；
- 对端基本文件名为 `.`、`..`、路径分隔符、盘符、UNC、设备前缀和平台保留名；
- Linux 合法但 Windows 非法、以及 Windows 合法但目标平台非法的默认文件名；
- 目标已存在；
- 父目录不存在；
- 权限不足；
- 空间不足；
- 源为目录或特殊文件；
- 符号链接最终指向普通文件；
- 提交前目标被竞争创建；
- 源文件传输中增长、缩短或改变；
- 临时文件清理；
- no-replace 提交；
- 正常成功与突然掉电持久性边界不混淆。

### 23.5 生命周期

覆盖：

- 提示阶段取消；
- 数据阶段取消；
- Finish 后取消；
- 本地主动脱离；
- 网络断开；
- relay 断开；
- Shell 退出；
- 双方进程终止；
- 控制交换超时；
- 数据无进展超时；
- 文件失败后终端继续可用；
- 文件大流量下 terminal-data、terminal-control、取消和关闭持续推进；
- 传输时远端输出持续产生；
- 传输期间普通本地键盘不发送、不重放。

### 23.6 兼容与平台

覆盖：

- 0.1.3 ↔ 0.1.3；
- 0.1.3 主控端 ↔ 旧被控端，且不支持时不进入路径提示；
- 旧主控端 ↔ 0.1.3 被控端；
- 直连；
- 普通 relay；
- 0.1.2 企业 relay；
- 六个正式 target 构建；
- Linux、Windows、macOS 两两跨系统上传和下载。

---

## 24. 发布验收标准

0.1.3 发布前必须全部满足：

1. `Ctrl+] u` 可以上传一个普通文件；
2. `Ctrl+] d` 可以下载一个普通文件；
3. `Ctrl+] ?` 可以显示本地快捷键帮助；
4. 两端只需要 `yon`；
5. 不安装、不创建、不执行 `rz/sz` 或其他外部工具；
6. 文件字节不经过 PTY；
7. relay 不存储或理解文件；
8. 相对路径严格使用已定义的本地或远端基准目录；
9. 目标存在时始终拒绝；
10. 失败、取消和断线后没有最终半成品；
11. 成功文件通过大小和 SHA-256 验证；
12. 传输失败后终端继续可用；
13. 路径提示期间远端输出不会破坏输入行；
14. 路径提示缓冲达到上限时安全取消而不关闭终端；
15. 传输期间不做易受远端控制序列破坏的动态进度重绘；
16. 非交互终端字节完全透明；
17. 新旧端点兼容符合本文；
18. 普通 relay 和企业 relay 均通过；
19. 六个正式 target 均通过；
20. 日志不含路径、文件名、摘要或内容；
21. 协议输入有界且不能无界分配；
22. 默认文件名不能产生目录穿越、路径前缀或平台保留名写入；
23. 快捷键后已读取余量不得在文件操作结束后误发到远端；
24. 文件流不得使终端控制与关闭路径饥饿；
25. 不支持文件协议的旧端点不会触发路径提示或传输状态；
26. 文件协议错误只结束当前文件操作，不污染活动终端；
27. Shell 内临时提权不会被文件传输继承；
28. 0.1.1 和 0.1.2 回归测试全部通过。

---

## 25. 被排除的方向

### 25.1 独立 `yon upload` / `yon download`

形成第二套连接入口和生命周期，割裂当前终端使用上下文。

### 25.2 `rz/sz` 与 ZMODEM

要求外部工具、命令入口、PATH 或 Shell 参与，跨平台行为受外部实现影响。

### 25.3 Shell 注入或临时命令文件

改变 Shell 启动环境，存在平台和 Shell 差异，也会制造额外清理与命令冲突问题。

### 25.4 解析用户输入的 Shell 命令

无法可靠判断提示符和上下文，会与编辑器、REPL、TUI 和脚本冲突，破坏终端透明性。

### 25.5 抢占普通单键

`Ctrl+U`、`Ctrl+D`、Alt 和功能键都可能属于远端 Shell 或应用。直接抢占会破坏“像本地终端一样”的体验。

### 25.6 文件字节复用 terminal-data

混淆终端和文件语义，无法独立验证、取消、清理和报告错误。

### 25.7 文件管理器、目录和同步

超出 0.1.3 “简单、实用、单功能”的版本目标。

---

## 26. 全量交叉审计

本设计已在内部按“一问一答”的方式逐项决定快捷键、范围、路径、交互、协议、状态、失败、安全、跨平台和兼容规则；每项决定均与 0.1.1、0.1.2 及此前全部 0.1.3 决定叠加审计。以下为最终整体结论。

### 26.1 哲学统一：通过

Yonder 仍然是以真实、透明、跨平台远程终端为核心的产品。

0.1.3 的完整体验是：

```text
活动终端
→ 本地控制快捷键
→ 原生单文件传输
→ 返回同一终端
```

没有扩张为文件管理器、同步系统、Shell 解析器或外部协议兼容层。

### 26.2 语义一致：通过

- terminal-data 只承载终端字节；
- terminal-control 只承载终端控制；
- file-transfer 只承载一次单文件传输；
- `Ctrl+]` 后的选择器只表示本地主控会话操作；
- 上传始终是主控端到被控端；
- 下载始终是被控端到主控端；
- 相对路径始终相对于固定、可重现的会话基准目录；
- 企业认证只负责建立会话，不进入文件协议。

不存在同一动作多种入口，也不存在 Shell 命令与本地控制混用。

### 26.3 逻辑自洽：通过

成功闭环：

```text
已认证活动终端
→ 显式快捷键
→ 本地路径提示
→ 同连接打开文件子流
→ 接收方创建临时文件
→ 流式传输
→ 大小和摘要验证
→ no-replace 提交
→ 返回活动终端
```

失败闭环：

```text
失败或取消
→ 关闭文件子流
→ 删除临时文件
→ 不创建最终半成品
→ 返回活动终端
```

提示期间的远端输出有界延迟，传输期间远端输出继续实时排空，因此既不破坏本地输入行，也不阻塞远端 PTY。

### 26.4 真实有效：通过

设计基于：

- 现有 `Ctrl+]` 本地前缀状态机；
- 现有端点之间的应用子流能力；
- 同一个 `yon` 二进制；
- 真实文件系统和跨平台文件 API；
- 明确的相对路径基准；
- 流式有界传输；
- 可实现的安全临时文件与 no-replace 提交。

设计没有假设能够可靠解析 Shell、跨平台获取 Shell 当前目录、无入口实现裸 `rz/sz`、让文件系统自动提供快照，或让网络永不掉线。

### 26.5 完整可靠：通过

设计完整覆盖：

- 产品边界；
- 用户入口；
- 本地控制状态机及模态状态控制；
- 路径提示与远端输出协调；
- 上传和下载状态机；
- 完整 wire 帧、tag、载荷和错误码；
- 文件、路径和跨平台基本文件名语义；
- 安全临时写入；
- no-replace 提交；
- 大小和摘要验证；
- 用户取消与文件子流终止；
- 连接与 Shell 生命周期；
- 有界资源与终端公平性；
- 能力探测与新旧版本；
- 普通与企业 relay；
- 六个平台；
- 测试和发布验收。

没有遗留会改变 0.1.3 产品形态或外部语义的待决问题。实现阶段可以决定内部 Rust 类型、模块拆分、固定超时数值和经审计的依赖版本，但不得重新解释本文冻结契约。

---

## 27. 最终冻结基线

Yonder 0.1.3 正式冻结为：

> Yonder 在已经建立的交互式远程终端会话中提供原生、端到端、单文件上传与下载。用户使用 `Ctrl+] u` 上传、使用 `Ctrl+] d` 下载，并可使用 `Ctrl+] ?` 查看本地控制帮助。文件通过独立 `/yonder/file-transfer/1.0.0` 子流传输，不经过 PTY，不依赖 Shell、`rz/sz`、ZMODEM 或任何额外软件。每次只传一个普通文件，目标必须不存在，接收方通过安全临时文件、大小验证、SHA-256 和 no-replace 提交保证完整结果。文件传输继承当前活动终端会话的认证、连接、权限和生命周期，普通文件错误返回原终端，不改变 relay 或 0.1.2 企业认证模型。

同时冻结：

> 所有依附于活动交互会话、由主控端本地触发且不应发送到远端 PTY 的 Yonder 能力，统一优先设计为 `Ctrl+]` 后接一个固定单字符的本地控制操作。任何新增选择器都必须由版本设计明确分配，并重新完成兼容性分析和五要素全量交叉审计。

本文可直接作为 Yonder 0.1.3 的实现、任务拆分、代码审查、协议测试和发布验收基线。
