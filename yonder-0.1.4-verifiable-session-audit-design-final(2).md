# Yonder 0.1.4 Verifiable Session Audit Design

**状态：Final Design Baseline**  
**版本：0.1.4**  
**前置基线：Yonder 0.1.1、0.1.2、0.1.3 最终设计基线**  
**设计主题：纯端点、双端自动、失败关闭的可验证终端会话审计**

---

## 1. 最终结论

Yonder 0.1.4 只增加一个简单、有效、企业可用的能力：

> 每一次成功进入远程终端的会话，都由主控端和被控端的 `yon` 自动、独立地生成本地审计记录。两端记录通过密码学哈希链、双端交叉检查点、会话最终共同清单和签名证明彼此一致；审计查看者能够安全重放当时的远程终端画面，并核验记录是否完整、是否被修改、是否来自同一次会话。

0.1.4 不增加会话中的用户操作步骤：

```text
yon host
yon connect
```

仍然是完整使用方式。

用户不需要：

- 显式开启审计；
- 输入审计命令；
- 按新的会话快捷键；
- 修改 Shell；
- 安装第三方录像程序；
- 配置 relay；
- 操作中心审计服务。

审计只存在于两个 `yon` 端点。`yon-relay` 不读取、不保存、不验证审计内容，也不增加审计配置。

0.1.4 的审计是强制能力：

- 任一端无法在本地建立审计记录时，不得进入 Terminal Active；
- 会话过程中任一端无法继续可靠记录时，必须有序终止会话；
- 双端审计协议出现不一致时，必须立即终止会话；
- 不允许终端继续运行后再标记为“本次未审计”。

---

## 2. 产品定位

### 2.1 0.1.4 是什么

0.1.4 是：

- 自动会话审计；
- 双端独立取证；
- 终端事实记录；
- 篡改检测；
- 双端一致性认证；
- 本地留存；
- CLI 验证；
- 安全终端重放。

### 2.2 0.1.4 不是什么

0.1.4 不是：

- 中心审计平台；
- Web 管理后台；
- SIEM；
- 堡垒机审批系统；
- 命令风险识别系统；
- Shell 命令解析器；
- 用户行为分析；
- 告警平台；
- 审计检索数据库；
- 长期归档服务；
- relay 录像；
- 第三方可信时间戳服务；
- 可信硬件证明系统。

### 2.3 核心价值

0.1.4 必须能够回答：

1. 提供出来的审计文件是否具有完整、可验证的结构；
2. 记录是否被修改、插入、删除、调序或截断；
3. 主控端交给 Yonder 发送路径的终端字节，与被控端从 Yonder 接收路径取得的字节是否一致；
4. 被控端从 PTY/ConPTY 读取并交给 Yonder 发送路径的输出，与主控端从 Yonder 接收路径取得的输出是否一致；
5. Yonder 是否成功把输入交付 PTY/ConPTY、把输出交付本地显示写入路径，或者在哪一步失败；
6. 会话何时建立、如何结束；
7. 会话中发生了哪些 Yonder 原生操作；
8. 审计人员能否安全重建主控端收到并提交给本地终端显示的主要画面；
9. 对于未回显输入，是否能够证明输入确实发生且双端一致，同时明确其原文不可恢复。

审计证明的是 Yonder 可观察边界中的事实，不能证明人眼实际看到了每一个字符，也不能证明某段终端输入最终被 Shell 解释成了什么命令。

---

## 3. 冻结原则

0.1.4 遵守以下原则：

1. **用户无感**：不改变 `yon host` 和 `yon connect` 的使用方式。
2. **功能旁路**：审计观察现有终端和文件传输边界，不重解释业务语义。
3. **纯端点实现**：主控端和被控端负责记录与认证，relay 不参与。
4. **双端独立**：两端分别生成自己的审计文件，任何一端不能代替另一端记录。
5. **事实优先**：记录 Yonder 实际发送、接收和执行的协议事实。
6. **不解析 Shell**：不把终端字节猜测为权威命令语义。
7. **输入保密**：不在审计文件中保存原始键盘输入内容。
8. **画面可见**：完整保存终端输出，以便重建操作人员看到的画面。
9. **一致与认证分层**：双端记录、双端签名和全部共享事实一致，只能证明所提供文件彼此自洽；只有至少一端同时匹配受保护的持久身份和账本连续性，才输出完整认证。
10. **失败关闭**：无法继续审计时不能继续远程操作。
11. **不降低旧能力**：终端、认证、relay 和文件传输的既有外部语义保持不变。
12. **边界诚实**：不宣称在没有第三方见证时能够证明两端记录被整体删除的会话曾经存在。

---

## 4. 权威审计对象

### 4.1 终端会话事实流

审计的权威对象是 Yonder 实际观察到的事实，而不是对 Shell 的推测。

包括：

- 端到端认证完成；
- 审计握手完成；
- TerminalHello；
- TerminalReady；
- 主控端发送的终端输入字节流摘要；
- 被控端收到的终端输入字节流摘要；
- 被控端产生的终端输出字节；
- 主控端收到的终端输出字节；
- 窗口尺寸变化；
- 终端退出码；
- TerminalComplete；
- 本地主动脱离；
- 本地中断；
- 连接中断；
- 审计异常；
- `Ctrl+]` 本地控制动作；
- 0.1.3 文件上传、下载、取消和结果；
- 后续版本定义的其他 Yonder 原生会话操作。

### 4.2 不解析 Shell 命令

Yonder 不可靠地知道：

- 当前是否处于 Shell；
- 用户是否在 Vim、数据库客户端、REPL 或 TUI 中；
- 一行输入是否已经执行；
- alias、函数、变量、管道和脚本如何展开；
- `sudo` 或 `su` 后的内部执行身份；
- 关闭回显时输入的内容属于密码还是其他协议。

因此：

> 审计文件不包含“用户执行了某条 Shell 命令”这一权威事件，除非该动作本身是未来 Yonder 原生协议明确定义的结构化操作。

审计人员通过终端画面重放观察用户当时做了什么。对于普通交互式 Shell，命令通常会由远端终端回显并出现在画面中。

必须明确：当远端程序没有回显输入时，0.1.4 只能证明某段输入在某个时间发生、长度是多少、双端摘要是否一致，不能向审计人员展示其原文。这是避免把密码、Token 和其他隐藏凭证永久写入审计的有意安全边界，不得在产品文案中宣称‘所有键入内容均可查看’。

### 4.3 重放的定义

“重放”仅表示：

- 按主控端本地单调时间线；
- 重建窗口尺寸变化；
- 重建远端输出形成的终端画面；
- 显示 Yonder 原生操作事件；
- 显示会话建立、结束和异常状态。

重放绝不：

- 重新向远端发送输入；
- 重新执行命令；
- 重新发起文件传输；
- 重新连接原目标；
- 修改本地或远端文件系统。

---

## 5. 输入保密与可验证性

### 5.1 不保存原始输入内容

0.1.4 不在任何审计文件中保存主控端原始键盘输入字节。

原因：

- 终端输入可能包含密码；
- 可能包含 API Token、私钥口令和数据库凭证；
- 跨平台无法可靠判断当前是否关闭回显；
- 依赖 Shell、PTY 模式或界面状态进行选择性记录会产生遗漏和错误判断。

### 5.2 禁止使用裸输入摘要

不得把 `SHA-256(input_bytes)` 直接写入审计文件。

短密码、PIN、常见 Token 和短命令的搜索空间有限，裸摘要会形成离线字典验证器，即使没有保存原文也可能泄露秘密。

### 5.3 会话私有输入承诺密钥

双方在审计握手中各自生成 32 字节随机秘密贡献，并通过已经认证、端到端加密的审计子流交换。

每个 AuditHello 只持久记录并签署本端秘密贡献的 SHA-256 承诺；秘密贡献本身：

- 不写入 `.yonaudit`；
- 不进入普通日志；
- 不发送给 relay 明文；
- 只存在于本次会话内存；
- 会话结束后销毁。

双方按角色固定顺序使用 HKDF-SHA-256 派生：

```text
input_commitment_key =
HKDF-SHA-256(
    authenticated_connection_secret,
    controller_secret_contribution || host_secret_contribution,
    "yonder-audit-input-commitment-v1" || session_id
)
```

如果现有安全会话不能提供可导出的认证秘密，则使用双方秘密贡献作为 HKDF 输入密钥材料，并把认证连接绑定、双方 nonce 和 session ID 放入 HKDF context；不得把派生密钥或秘密贡献持久化。

### 5.4 输入如何进入双端认证

输入仍然进入双端一致性认证。

每个规范化输入块记录：

- 方向；
- 单调序号；
- 字节数；
- `HMAC-SHA-256(input_commitment_key, domain || direction || sequence || length || bytes)`；
- 共享输入链的新链头；
- 本地观察时间。

主控端在发送前计算；被控端在写入 PTY/ConPTY 前计算。

因此可以证明：

```text
controller committed input block N
==
host committed the received input block N
```

离线验证器不需要输入承诺密钥，只比较双方被签名和哈希链保护的承诺值。由于密钥不在文件中，审计文件不能被直接用作输入内容的离线字典验证器。

### 5.5 审计人员看到什么

审计人员能够看到：

- 完整远端终端输出形成的主要画面；
- Shell 正常回显的命令；
- 程序输出；
- 屏幕变化；
- 文件传输方向、路径、大小、摘要和结果；
- 本地控制动作；
- 输入事件发生的时间、长度与一致性状态。

未回显输入只显示为：

```text
[input: 12 bytes, content protected, bilateral commitment matched]
```

不显示原始内容，也不显示可供离线猜测的裸内容摘要。

---

## 6. 总体架构

```text
┌────────────────────────────┐
│ controller yon             │
│                            │
│ existing terminal logic    │
│ existing file transfer     │
│ audit observer             │
│ local audit writer         │
│ local audit identity       │
└──────────────┬─────────────┘
               │
               │ existing authenticated
               │ end-to-end connection
               │
               │ /yonder/audit/1.0.0
               │
┌──────────────▼─────────────┐
│ host yon                   │
│                            │
│ existing PTY/ConPTY logic  │
│ existing file transfer     │
│ audit observer             │
│ local audit writer         │
│ local audit identity       │
└────────────────────────────┘
```

relay 只观察到原有加密连接和流量元数据，不理解审计明文。

---

## 7. 对既有版本的继承

### 7.1 继承 0.1.1

保持：

- 连接码；
- OPAQUE；
- 直连和 relay 回退；
- PTY/ConPTY；
- TerminalHello、TerminalReady、TerminalExit 和 TerminalComplete；
- 终端输入输出透明语义；
- `Ctrl+] .`；
- `Ctrl+] Ctrl+]`；
- 非交互输入透明；
- Shell 和当前用户权限边界。

### 7.2 继承 0.1.2

保持：

- 普通 Resolve 和 Enterprise Resolve；
- 企业认证边界；
- 企业模式不得回退为普通 Resolve；
- relay 不理解端到端业务内容；
- 企业身份只使用现有认证流程已经提供的身份事实；
- 审计不新增企业身份来源。

若 0.1.2 会话上下文中已经存在经过认证的企业主体或端点标识，0.1.4 将其写入双方共同清单。普通模式中只记录审计身份指纹和本次会话绑定。

### 7.3 继承 0.1.3

保持：

- `Ctrl+] u`；
- `Ctrl+] d`；
- `Ctrl+] ?`；
- 原生单文件传输；
- 独立文件子流；
- 不经过 PTY；
- 安全临时文件；
- SHA-256；
- no-replace 提交；
- 文件操作失败不结束终端。

0.1.4 只旁路记录文件传输结构化事件，不记录文件内容。

---

## 8. 不改变使用方式的准确含义

0.1.4 不改变用户操作和已有功能语义，但实现必须在既有边界增加审计观察点。

允许的代码修改：

- 在输入发送前调用审计记录器；
- 在输入写入 PTY 前调用审计记录器；
- 在输出发送前调用审计记录器；
- 在输出显示前调用审计记录器；
- 在尺寸、生命周期和文件事件发生时提交结构化审计事件；
- 在 Terminal Active 前完成审计握手；
- 在会话结束前完成审计最终化。

禁止：

- 修改终端字节内容；
- 修改终端字节顺序；
- 解析 Shell；
- 用新的终端实现替换旧实现；
- 复制并分叉一套“审计模式终端”；
- 让审计线程直接控制 PTY；
- 让审计改变文件传输语义；
- 让 relay 解析审计数据。

实现应使用独立、窄接口的审计观察器包装既有边界，而不是重写原状态机。

---

## 9. 审计身份

### 9.1 持久审计身份

每个本地 `yon` 用户环境拥有一把持久 Ed25519 审计身份密钥。

用途：

- 证明多次会话属于同一端点审计身份；
- 签署会话临时密钥；
- 签署本地审计账本提交；
- 防止仅替换单个审计文件而重新生成一套自洽记录。

### 9.2 自动创建

不增加初始化命令。

第一次使用 0.1.4 且本地不存在审计历史时：

1. 自动生成 Ed25519 密钥；
2. 创建私有审计目录；
3. 排他写入审计身份；
4. 同步身份文件和目录；
5. 初始化空审计账本。

若审计历史存在但身份文件缺失、不可读或无效，必须失败，不能静默生成新身份。

### 9.3 会话临时签名密钥

每次会话双方各自生成一把临时 Ed25519 会话密钥。

- 持久审计身份签署临时公钥和会话绑定；
- 周期检查点和最终共同清单由临时密钥签署；
- 会话完成或失败后销毁临时私钥；
- 临时私钥不写入磁盘。

这样，事后修改事件不能通过重新计算会话签名恢复为有效记录。

### 9.4 身份边界

在没有企业 PKI、中心登记、外部公钥清单或本地受保护身份锚点时，文件内嵌的持久公钥只能证明这两份文件在其自声明密钥体系下相互签署和内部一致，不能证明它们就是最初产生于真实会话的原始文件。

0.1.4 区分两种结论：

- **未锚定完整一致**：双端完整记录、双方嵌入身份签名、会话签名和全部共享事实链一致。它证明所提供的两份文件形成一套密码学自洽的完整记录，但不能排除攻击者事后连同两套新身份一起重新生成整对文件。
- **完整认证**：在未锚定完整一致的基础上，至少一端还匹配当前受保护的本地持久审计身份和账本连续性。它进一步证明记录属于该已知端点的连续历史。

因此：

- 第三台没有任何可信锚点的机器最多输出 `CONSISTENT_COMPLETE_UNANCHORED`；
- 至少匹配一端受保护身份和账本时，才可以输出 `VERIFIED_COMPLETE`。

即使达到 `VERIFIED_COMPLETE`，密码学身份也不会自动等同于自然人或企业资产身份；该映射只能来自既有企业认证上下文或未来外部信任清单。

---

## 10. 本地存储位置

审计属于运行用户状态，不属于配置。

### 10.1 Linux

```text
$XDG_STATE_HOME/yonder/audit
```

若 `XDG_STATE_HOME` 未设置：

```text
~/.local/state/yonder/audit
```

`XDG_STATE_HOME` 已设置时必须是非空绝对路径；否则审计初始化失败，不按当前目录猜测。

### 10.2 macOS

```text
~/Library/Application Support/Yonder/Audit
```

### 10.3 Windows

```text
%LOCALAPPDATA%\Yonder\Audit
```

`LOCALAPPDATA` 必须能解析为非空绝对路径；异常时审计初始化失败。

### 10.4 目录结构

```text
Audit/
├── identity.ed25519
├── ledger.state
├── ledger.lock
└── records/
    ├── <session-id>.controller.yonaudit
    └── <session-id>.host.yonaudit
```

每台机器通常只保存本机角色对应的一份文件。

文件名中的时间不是身份依据；真正唯一性来自随机会话 ID。

---

## 11. 本地文件保护

### 11.1 Unix

- 审计根目录和 `records`：`0700`；
- 身份、账本、`ledger.lock` 和审计文件：`0600`；
- 创建时即应用权限；
- 不允许先以宽权限创建后再收紧；
- 符号链接不得作为身份、账本或记录目标；
- 所有文件排他创建；
- 权限异常时拒绝会话。

### 11.2 Windows

必须创建和验证受保护 DACL：

- 当前运行用户；
- `SYSTEM`；
- `Administrators`。

其他主体不得获得读取、写入或删除权限。

无法可靠创建或验证 DACL 时，不允许进入 Terminal Active。

### 11.3 不自动删除

0.1.4：

- 不自动轮转；
- 不自动删除；
- 不根据空间自动丢弃旧记录；
- 不提供无审计降级。

存储归档和保留期限属于后续版本或企业部署策略。

---

## 12. 本地审计账本

### 12.1 目的

双端文件可以证明共享事实一致，但持久身份与连续账本用于把会话绑定到已知端点的历史。

每个端点维护本地连续账本：

```text
ledger_root_0
→ session_commit_1
→ ledger_root_1
→ session_commit_2
→ ledger_root_2
```

每次提交包含：

- 本地账本序号；
- 前一账本根；
- 会话 ID；
- 最终共同清单摘要；
- 本地 sealed-record 摘要；
- 对端审计身份指纹；
- 会话结果；
- 持久审计身份签名。

### 12.2 并发会话与账本锁

同一运行用户可以同时运行多个 `yon` 进程。账本不得假设会话串行，也不得让两个并发会话从同一前序根各自提交形成分叉。

必须使用跨进程排他 `ledger.lock`，但只在以下短阶段持有：

- 首次身份和账本初始化；
- 待提交记录恢复；
- 会话最终账本提交。

不得在整个终端会话期间持锁。

AuditHello 中携带的账本序号和根只是“会话开始快照”，不是本次会话最终提交必然使用的前序根。最终提交必须在持有锁后，以当时最新账本头为前序根计算。

### 12.3 非循环最终化

正常会话结束按以下顺序执行：

1. 完成双端最终共同清单和双方会话签名；
2. 结束本地事件链，形成 `final_local_event_root`；
3. 使用本地会话临时密钥签署 `LocalRecordSeal`，其覆盖 session ID、角色、最终本地链根、全部共享链根、共同清单摘要和本地事件计数；
4. 计算 `sealed_record_digest`，覆盖 header、全部 record frame、共同清单、双方会话签名和 LocalRecordSeal，但不包含后续 ledger commit 和最终容器摘要；
5. 获取 `ledger.lock`，先恢复唯一待提交记录，再读取最新账本头；
6. 创建并由持久审计身份签署 ledger commit，ledger commit 引用 `sealed_record_digest`，不引用包含自身的最终容器摘要；
7. 将 ledger commit 和最终容器摘要追加到 footer；最终容器摘要覆盖其前全部容器内容但排除摘要字段自身；
8. `sync_all` 审计文件；
9. 原子替换 `ledger.state` 为新账本头并同步账本文件与父目录；
10. 释放锁并销毁临时会话私钥。

这样不存在“footer 包含 ledger commit，而 ledger commit 又签署包含自身的 footer”这一循环依赖。

### 12.4 崩溃恢复

如果进程在审计文件已经同步、但 `ledger.state` 尚未推进时崩溃：

- 下一个取得 `ledger.lock` 的进程必须先扫描完成且包含合法 ledger commit 的待提交记录；
- 由于最终化全程受同一锁串行化，最多允许一个记录直接连接当前账本头；
- 验证持久身份签名、前序根和 `sealed_record_digest` 后推进账本；
- 出现多个冲突候选、分叉或非法前序根时拒绝新会话。

未完成共同清单或未形成合法 LocalRecordSeal 的异常记录不能进入账本。

### 12.5 账本能力边界

本地账本能够发现：

- 保留较新账本头时删除中间会话；
- 将某个会话文件替换为不属于该链的文件；
- 账本提交被单独修改；
- 并发提交造成的非法分叉。

AuditHello 还会让对端记录本端会话开始时的账本快照，因此只回滚一端而保留对端后续记录时可以形成额外的不一致证据。

在没有外部锚点、可信单调计数器或第三方见证时，本地账本仍不能发现攻击者把账本状态、身份和记录集合一起回滚到某个更早但当时合法的完整快照。

---

## 13. 审计会话建立

### 13.1 协议

新增：

```text
/yonder/audit/1.0.0
```

并在 `yonder-core` 中新增：

```text
wire::audit
AUDIT_PROTOCOL
```

### 13.2 建立时机

审计协议在以下条件后建立：

- 目标解析完成；
- 端到端连接确定；
- OPAQUE 成功；
- 已获得现有认证会话绑定。

但必须在以下动作前完成：

- 创建或对用户开放远端终端；
- Terminal Active；
- 任意终端输入；
- 任意文件传输。

### 13.3 AuditHello 与秘密贡献交换

双方发送并持久记录的 AuditHello 公共字段包括：

- 协议版本；
- 角色；
- 持久审计公钥；
- 会话临时签名公钥；
- 32 字节随机 nonce；
- 会话开始时的本地账本序号与账本根快照；
- 认证连接绑定摘要；
- 支持的审计文件格式；
- 本端输入承诺秘密贡献的 SHA-256；
- 持久审计身份签名。

签名覆盖全部公共字段和明确域分离标签。

双方还通过同一已认证、端到端加密的审计子流交换实际的 32 字节秘密贡献。接收方必须验证其 SHA-256 与 AuditHello 中已签署的承诺一致。实际秘密贡献不得写入审计文件。

### 13.4 会话 ID

双方按固定顺序计算：

```text
session_id =
SHA-256(
    "yonder-audit-session-v1"
    || authenticated_connection_binding
    || controller_nonce
    || host_nonce
    || controller_audit_identity
    || host_audit_identity
    || controller_session_key
    || host_session_key
    || controller_input_secret_commitment
    || host_input_secret_commitment
)
```

### 13.5 AuditReady

双方必须：

1. 验证对端 AuditHello；
2. 验证对端秘密贡献与已签署承诺一致；
3. 计算相同 session ID；
4. 派生相同的会话私有输入承诺密钥；
5. 创建本地审计文件；
6. 写入并同步 header；
7. 交换由会话临时签名密钥签署的 AuditReady；
8. 确认双方 session ID、身份、输入承诺参数和格式一致。

只有双方 AuditReady 成功后，终端建立流程才能继续。

---

## 14. 旧版本兼容

0.1.4 的审计是强制、双端能力。

因此：

- 0.1.4 主控端连接旧被控端：在 Terminal Active 前明确失败；
- 旧主控端连接 0.1.4 被控端：在 Terminal Active 前明确失败；
- 不回退为单端记录；
- 不提供 `--no-audit`；
- 不提供“继续但未审计”；
- 不通过 Shell 或日志模拟审计。

这是 0.1.4 为保证‘每次成功会话都可验证审计’而引入的唯一有意互操作变化。0.1.4 双端之间的终端、认证、relay 和文件传输外部语义必须保持既有基线；但 0.1.4 与旧端点不能同时满足‘允许会话’和‘强制双端审计’，二者不可兼得。

固定错误语义：

```text
peer does not support mandatory verifiable session audit
```

relay 不需要升级，只要仍能透明承载应用子流。

---

## 15. 事件模型

### 15.1 两层事件

审计同时维护：

1. **共享事实链**：两端必须计算一致；
2. **本地观察链**：包含本地时间和本地私有上下文，只由本端签名。

### 15.2 共享事实链

至少包括：

- controller-to-host 终端输入承诺；
- host-to-controller 终端输出内容摘要；
- resize；
- TerminalHello 摘要；
- TerminalReady；
- TerminalExit；
- TerminalComplete；
- 文件传输开始；
- 文件传输完成、取消或失败；
- 会话关闭原因。

只有双方都能从协议事实独立得到的字段才能进入共享事实链。主控端本地帮助操作 `Ctrl+] ?` 只进入主控端本地观察链；`Ctrl+] u`、`Ctrl+] d` 只有在文件协议真正建立后才产生共享文件事件；`Ctrl+] .` 总是形成主控端本地事件，并在关闭原因成功传达给对端后形成共享关闭事件。

审计 Checkpoint、CheckpointAck 和最终化控制消息不是共享事实链事件。它们是对“检查点产生前”的共享链状态所作的签名证明，分别作为协议证据写入双方本地观察链。这样避免检查点把包含自身的链头作为输入而形成自引用。

### 15.3 本地观察链

至少包括：

- 本地单调时间；
- 本地墙钟时间元数据；
- 网络原始输出块；
- 经过本地平台输出适配后、实际提交给显示写入路径的显示字节块；
- 输入承诺事件；
- 本地快捷键动作；
- 本地文件路径；
- 本地 UI 状态；
- 本地审计错误；
- 本地连接状态变化。

### 15.4 时间

每端记录：

- UTC 墙钟开始和结束时间；
- 从 AuditReady 起算的本地单调纳秒或等价精度时间。

墙钟时间：

- 由本端签名；
- 用于查询和展示；
- 不视为可信时间戳；
- 不用于决定跨端事件总顺序。

跨方向并发事件不存在伪造的全局绝对顺序。主控端重放采用主控端本地观察顺序。

---

## 16. 字节流规范化

网络和操作系统读取边界不稳定，不能直接比较本地读取块。

### 16.1 规范块

终端输入和输出分别维护独立规范化器：

- 规范块大小：16 KiB；
- 按字节流顺序填充；
- 每满 16 KiB 形成一个共享块；
- 方向正常关闭时形成最后一个部分块；
- 空方向不产生数据块。

规范块只用于跨端比较，不得成为发送前必须凑满的缓冲。网络和 PTY 字节仍按既有读取节奏立即处理；每个本地观察段先写入本地审计，再更新规范化器。某个观察段使规范块完成时，在该观察段产生外部效果前写入完成块承诺；尚未完成的部分块只保存在有界内存中，并在正常方向关闭时最终化。

周期检查点只承诺双方已经形成的完整规范块。每个方向最多有不足 16 KiB 的当前部分块尚未进入共同检查点；异常断开时该尾部可以存在于本地观察记录中，但不能冒充已经双端确认的共享前缀。

### 16.2 输入共享块

记录：

- 方向；
- 序号；
- 长度；
- 使用会话私有输入承诺密钥计算的 HMAC-SHA-256；
- 前一输入共享链头；
- 新链头。

不记录输入内容、裸 SHA-256 或输入承诺密钥。

### 16.3 输出共享块

记录：

- 方向；
- 序号；
- 长度；
- SHA-256；
- 前一输出共享链头；
- 新链头。

两端本地审计文件仍分别保存各自观察到的完整输出字节，用于重放与本地链验证。

### 16.4 共享链不包含本地时间

共享链只包含规范化事实，不能包含双方不同的本地观察时间。

本地时间进入本地观察链。

---

## 17. 哈希链

所有哈希使用 SHA-256。

### 17.1 共享事件哈希

```text
shared_event_hash =
SHA-256(
    domain
    || previous_shared_hash
    || stream_kind
    || direction
    || sequence
    || event_kind
    || canonical_payload
)
```

### 17.2 本地事件哈希

```text
local_event_hash =
SHA-256(
    domain
    || previous_local_hash
    || local_sequence
    || monotonic_time
    || event_kind
    || local_payload
    || related_shared_event_hash
)
```

### 17.3 域分离

终端输入、终端输出、终端控制、文件操作、本地事件、检查点、共同清单和账本提交必须使用不同固定域标签。

---

## 18. 记录时机、效果结果与失败关闭

审计必须区分“Yonder 准备产生某个效果”和“底层写入最终是否成功”。前置记录保证失败不会静默越过审计，后置本地结果事件避免把收到字节错误地宣称为已经由 PTY、网络或本地终端成功消费。

### 18.1 主控端输入

```text
read local input
→ apply existing Ctrl+] state machine
→ append local input commitment event
→ update canonical input stream and append any completed shared block
→ audit append success
→ send terminal bytes
→ append local send outcome
```

### 18.2 被控端输入

```text
receive terminal bytes
→ append local input commitment event
→ update canonical input stream and append any completed shared block
→ audit append success
→ write PTY/ConPTY
→ append local PTY write outcome
```

### 18.3 被控端输出

```text
read PTY/ConPTY output
→ append local full-output event
→ update canonical output stream and append any completed shared block
→ audit append success
→ send terminal bytes
→ append local send outcome
```

### 18.4 主控端输出

```text
receive terminal raw bytes
→ append local raw-output event
→ update canonical output stream and append any completed shared block
→ run the existing platform output adapter
→ append local display-bytes event
→ audit append success
→ write display bytes to local terminal output path
→ append local display-write outcome
```

共享输出链始终比较网络传输的原始终端字节。重放时间线使用主控端本地 `display-bytes`，因为 Windows 控制台等平台适配可能把无效 UTF-8 转为替代字符；只重放网络原始字节不能严格代表当时提交给本地显示路径的内容。

正常完整会话必须具有与全部外部效果相匹配的成功 outcome。若底层写入失败或进程在 outcome 写入前终止，会话不能形成完整最终共同清单。

审计只能证明 Yonder 调用了并完成了对应写入接口，不能证明人眼实际看到了屏幕内容，也不能证明 PTY 内部程序如何解释输入。

### 18.5 Resize

发送方和接收方必须分别在发送或应用尺寸变化前记录事件，并在操作后记录本地 outcome。

### 18.6 文件传输

文件内容不进入审计。

共享文件事实只记录双方都能从 0.1.3 文件协议独立验证的字段：

- transfer ID；
- 方向；
- 远端协议路径；
- 协议基本文件名；
- 宣告大小；
- 最终大小；
- SHA-256；
- 开始、成功、取消或结构化错误码。

本端独有路径只进入本地观察链：

- 上传时，主控端本地源完整路径只存在于主控端记录；
- 下载时，主控端本地目标完整路径只存在于主控端记录；
- 被控端只记录由远端协议路径解析得到的本机路径；
- 双端验证不得要求本端独有路径彼此相同。

成功结果必须在双方记录成功事件后才显示给用户。

### 18.7 写入失败

任何前置同步 append 失败：

1. 停止继续产生可观察终端效果；
2. 发送审计失败关闭原因；
3. 取消文件传输；
4. 终止 PTY/ConPTY；
5. 恢复本地终端；
6. 将本地文件保留为可验证中断前缀；
7. 返回明确错误。

后置 outcome append 失败同样必须立即失败关闭，且该会话不得被认证为完整。

---

## 19. 审计写入模型

### 19.1 有界

审计 writer 可以使用独立任务，但必须：

- 使用有界队列；
- 调用方等待该事件已经写入本地文件缓冲；
- writer 错误同步返回；
- 队列满时对终端产生背压；
- 不丢弃；
- 不转为普通日志；
- 不在内存中无限累计。

### 19.2 持久性

- header 在 Terminal Active 前 `sync_all`；
- 事件不对每条执行 `fsync`；
- 检查点前同步当前完整审计块；
- 正常最终化时，在共同清单、双方签名、LocalRecordSeal 和 ledger commit 全部追加后对完整审计文件执行 `sync_all`；
- 随后原子推进 `ledger.state`，并同步账本文件和父目录。

“append-before-effect”保证的是进程内顺序和可检测写入错误，不等同于每个事件已经抵抗掉电。突然崩溃或掉电可能使已经发生的最后一小段终端效果只存在于操作系统缓存、对端记录或尚未共同确认的本地尾部中。

验证必须明确区分：

- 已写入并同步的本地前缀；
- 已由双方检查点共同确认的共享前缀；
- 未共同确认或可能未持久化的本地尾部。

任何未进入完整最终共同清单的会话都不能报告为 `VERIFIED_COMPLETE`。

---

## 20. 双端检查点

### 20.1 触发

满足任一条件时生成检查点：

- 自上次检查点起经过 1 秒；
- 新增共享事实达到 1 MiB；
- TerminalReady；
- 文件传输开始或结束；
- 终端退出；
- 主动脱离；
- 会话准备关闭。

### 20.2 非自引用快照

Checkpoint 快照的是“生成该 Checkpoint 之前”的状态：

- session ID；
- 各共享流事件数；
- 各共享流 pre-checkpoint 链头；
- 本地观察链 pre-checkpoint 链头；
- 会话开始账本快照摘要；
- 检查点序号。

发送方用会话临时签名密钥签署 Checkpoint，然后才把 Checkpoint 证据追加到自己的本地观察链。Checkpoint 本身不进入被快照的共享链或本地链头。

### 20.3 确认

接收方必须：

1. 验证 Checkpoint 签名；
2. 对比自身相同共享流的计数和链头；
3. 把对端 Checkpoint 追加到本地观察链并同步当前完整文件块；
4. 生成 `CheckpointAck`，包含 session ID、检查点序号、Checkpoint 摘要和接收方相同共享链快照；
5. 在本地记录并同步 CheckpointAck 后发送其会话签名。

发送方验证并记录 CheckpointAck 后，该检查点才成为双方共同确认检查点。

CheckpointAck 同样只属于本地协议证据链，不进入其所确认的共享事实链。

出现共享根、计数、序号、Checkpoint 摘要或 session ID 不一致时：

- 记录 mismatch；
- 立即终止会话；
- 不继续终端操作。

### 20.4 异常中断

如果没有最终共同清单，双端文件仍可以核验最后一个双方共同确认检查点。

该状态只能报告为：

```text
MATCHED_INTERRUPTED_PREFIX
```

不能报告为完整认证。

---

## 21. 最终共同清单与本地记录封印

### 21.1 最终共同清单

正常结束时双方构造完全相同的共同清单：

- 格式版本；
- session ID；
- controller 持久审计身份指纹；
- host 持久审计身份指纹；
- controller 会话公钥；
- host 会话公钥；
- 认证连接绑定摘要；
- 已认证企业主体和端点标识的规范化共同部分（如双方均可验证）；
- TerminalHello 摘要；
- 所有共享事实链最终根；
- 所有共享流最终计数；
- 文件传输事件最终根；
- 终端退出码或关闭原因；
- 会话是否正常完成；
- 最终共同确认检查点序号。

只允许双方能独立得到完全相同规范值的认证字段进入共同清单；单端私有声明只能进入本地链。

### 21.2 双端会话签名

- controller 会话临时签名密钥签名；
- host 会话临时签名密钥签名；
- 双方都必须验证对端签名；
- 两个签名和共同清单写入各自记录。

### 21.3 LocalRecordSeal

共同清单不能包含两端不同的本地观察链根。每端必须额外创建本地记录封印：

```text
LocalRecordSeal {
    session_id,
    role,
    final_local_event_root,
    local_event_count,
    final_shared_roots,
    joint_manifest_digest,
    sealed_prefix_digest
}
```

LocalRecordSeal 由本端会话临时签名密钥签署。`sealed_prefix_digest` 覆盖截至封印前的 header 和全部 record frame；LocalRecordSeal 本身位于 footer，不进入 `final_local_event_root`，因此不存在自引用。

### 21.4 完成语义

只有以下步骤全部成功，在线会话才算审计完成：

1. 双方共享事实链一致；
2. 双方构造相同共同清单；
3. 双方会话签名有效；
4. 双方都确认对端共同清单签名；
5. 本地 LocalRecordSeal 有效；
6. 本地审计文件完成并同步；
7. 本地账本提交完成。

任一端最终化失败时，会话结果必须报告审计失败；不得只返回远端 Shell 退出码。

---

## 22. 关闭与退出语义

### 22.1 正常 Shell 退出

- 记录 TerminalExit；
- 完成剩余规范块；
- 交换最终检查点；
- 交换共同清单和签名；
- 提交本地账本；
- 完成原有 TerminalComplete；
- 返回原远端退出码。

审计最终化失败时，不得仅返回远端退出码，必须返回审计失败。

### 22.2 `Ctrl+] .`

- 记录本地主动脱离；
- 通知对端关闭原因；
- 尽力完成最终共同清单；
- 终止文件传输；
- 按既有语义结束终端；
- 审计成功时记录为正常主动脱离。

### 22.3 本地 `Ctrl+C`

按照既有会话语义处理，并记录本地中断原因。

### 22.4 网络断开

- 双方各自写入连接中断；
- 完成本地可用尾部；
- 保留最后共同检查点；
- 不伪造共同最终清单；
- 文件状态为中断；
- 双文件最多认证到最后共同检查点。

### 22.5 进程崩溃或掉电

- 文件可能只有有效前缀；
- 不存在有效 footer 时不得视为完整；
- 下次验证报告最后有效本地块和最后共同检查点；
- 不自动补写虚构的关闭事件。

---

## 23. 审计文件格式

### 23.1 文件类型

扩展名：

```text
.yonaudit
```

格式是版本化二进制容器，不使用 JSON 或文本行。

### 23.2 Header

至少包含：

- magic；
- 容器版本；
- 角色；
- session ID；
- 持久审计公钥；
- 临时会话公钥；
- 对端公钥；
- 本地账本序号；
- 前一本地账本根；
- UTC 开始时间；
- 认证模式；
- TerminalHello 摘要；
- AuditHello 和 AuditReady；
- header 签名。

### 23.3 Record Frame

每条记录使用：

```text
u32 big-endian frame_length
u8 record_type
payload
```

其中 `frame_length` 包含 `record_type` 和 payload，必须满足：

```text
1 <= frame_length <= 1_048_576
```

要求：

- 严格长度验证；
- 拒绝未知关键类型；
- 拒绝尾随字节；
- 所有计数和长度防溢出；
- 原始输出数据单 frame payload 最大 64 KiB；
- 输入只存承诺，不存内容；
- 解析不得按声明文件总大小分配内存。

### 23.4 Footer

正常完成的 footer 按固定顺序包含：

1. 最终共同清单；
2. 双方共同清单会话签名；
3. 本端 LocalRecordSeal 及其会话签名；
4. 持久身份签署的 ledger commit；
5. 最终容器摘要。

`sealed_record_digest` 只覆盖第 1 至第 3 项以及此前全部容器内容；ledger commit 引用该摘要。最终容器摘要覆盖 ledger commit 在内的全部前序内容，但排除摘要字段自身。二者不得互相引用形成循环。

异常文件可以没有 footer，但必须可验证完整前缀和最后共同确认检查点。

---

## 24. 审计内容边界

### 24.1 记录

- 网络原始终端输出；
- 主控端经过平台输出适配后提交给本地显示路径的显示字节；
- 输入块长度和会话私有 HMAC 承诺；
- 尺寸；
- 会话生命周期；
- 本地快捷键动作；
- 文件传输方向；
- 本地和远端文件路径；
- 文件大小；
- 文件 SHA-256；
- 结构化错误码；
- 认证模式；
- 已有企业身份标识；
- 审计身份指纹；
- 双端检查点和签名。

### 24.2 不记录

- 原始键盘输入；
- 连接码；
- OPAQUE 密码或内部状态；
- 会话加密密钥；
- 临时审计私钥；
- 文件内容；
- relay TLS 私钥；
- relay 配置值；
- 完整网络地址；
- 无关应用日志；
- 内部未整理推理。

### 24.3 审计文件本身是敏感数据

审计记录可能包含：

- 终端输出中的业务数据；
- 文件路径；
- 主机信息；
- 错误信息；
- 企业身份标识。

因此只能由有权限的本地用户或管理员读取。0.1.4 不实现基于角色的审计访问控制平台。

---

## 25. CLI

现有会话命令不变。

新增两个离线命令：

```text
yon audit verify <LOCAL_FILE> <PEER_FILE>
yon audit replay <CONTROLLER_FILE> [PEER_FILE]
```

### 25.1 `audit verify`

完整验证：

- 两个容器格式；
- 两个本地事件链；
- 输入共享链；
- 输出共享链；
- 控制共享链；
- 文件操作共享链；
- 双端检查点；
- 临时会话签名；
- 最终共同清单；
- 文件中嵌入的持久身份签名；
- 当前机器存在匹配身份时的可选本地账本连续性锚定；
- session ID、角色和身份对应关系。

### 25.2 验证状态

#### `VERIFIED_COMPLETE`

要求：

- 双端完整文件；
- 双端共同清单相同；
- 双方嵌入持久身份对本次会话绑定的签名有效；
- 双端会话签名有效；
- 全部共享事实链一致；
- 两份文件的本地链和 LocalRecordSeal 分别有效；
- 至少一端匹配当前受保护的持久审计身份和账本连续性。

它证明双端记录完整一致，并锚定到至少一个已知端点的连续历史。

#### `CONSISTENT_COMPLETE_UNANCHORED`

- 满足双端文件完整性、共同清单、双方嵌入签名、共享链和本地封印的一致性检查；
- 当前验证环境没有可信持久身份或账本锚点。

它只证明所提供的文件对密码学自洽，不能排除整对文件和新身份被事后共同生成。

#### `MATCHED_INTERRUPTED_PREFIX`

- 双端最后共同检查点一致；
- 会话异常中断；
- 之后可能存在本地未确认尾部；
- 不存在完整共同清单。

#### `INTACT_UNPAIRED`

- 单个文件自身链、已存在签名和本地封印有效；
- 没有提供对端文件；
- 不能认证双端一致。

#### `MISMATCH`

- 两端共享链、计数、身份、检查点或共同清单不一致。

#### `TAMPERED`

- 容器、哈希链、LocalRecordSeal、签名或已提供的可信账本证明无效。

缺少可信账本锚点本身不是 `TAMPERED`，而是未锚定状态。

### 25.3 退出码

- `0`：`VERIFIED_COMPLETE`；
- `1`：格式错误、I/O 错误；
- `2`：`CONSISTENT_COMPLETE_UNANCHORED`、`MATCHED_INTERRUPTED_PREFIX` 或 `INTACT_UNPAIRED`；
- `3`：`MISMATCH`；
- `4`：`TAMPERED`.

### 25.4 `audit replay`

- controller 文件是画面时间线来源；
- 提供 peer 文件时先验证并显示其准确状态；只有 `VERIFIED_COMPLETE` 或 `CONSISTENT_COMPLETE_UNANCHORED` 才允许完整会话重放；
- 未提供 peer 文件时必须显示 `INTACT_UNPAIRED`；
- 文件被篡改时拒绝重放；
- 原始输入内容不存在，不能显示；
- 文件操作显示为结构化时间线事件；
- `Ctrl+C` 退出重放。

---

## 26. 安全重放

不能把经过验证的远端输出原样无条件写入本地真实终端。

原因：

- 远端输出可能包含 OSC 52 剪贴板操作；
- 可能修改窗口标题；
- 可能包含设备查询；
- 可能包含 DCS、APC、PM 或其他终端副作用；
- “记录未被篡改”不等于“记录内容安全”。

### 26.1 内置安全终端模拟器

`yon audit replay` 必须：

- 使用 controller 文件中的本地 display-bytes 时间线，而不是直接使用 host 原始输出或共享链摘要；
- 使用内置 VT 解析器；
- 在内存中维护虚拟屏幕；
- 只把渲染结果转换为本地 crossterm 显示操作；
- 不把原始远端转义序列直接透传；
- 忽略或可见标记剪贴板、标题、设备控制和未知副作用序列；
- 不响应远端设备查询；
- 不访问网络；
- 不执行本地命令。

### 26.2 重放准确性边界

重放目标是安全还原主控端当时提交给本地显示路径的主要终端画面；它仍不能证明人眼实际看到了每次写入。

对于被安全过滤的副作用控制序列，审计文件保留原始输出，查看器显示“已过滤控制序列”事件，但不执行该副作用。

---

## 27. 性能和资源

### 27.1 内存

- 有界 writer 队列；
- 输出按最大 64 KiB frame 写入；
- 字节流规范化缓冲固定为 16 KiB；
- 检查点状态固定大小；
- 不把完整会话读入内存；
- verify 和 replay 流式处理。

### 27.2 磁盘

终端输出全部记录，因此高输出会话会消耗相应磁盘空间。

不设置产品级记录大小上限，也不丢弃旧事件。空间不足时会话失败关闭。

### 27.3 延迟

每个可观察终端动作在生效前等待本地 append 成功，因此会增加一次本地顺序写入成本。

不得：

- 每个字节 `fsync`；
- 为审计等待远端逐事件确认；
- 在终端热路径执行昂贵压缩；
- 将文件传输数据复制进审计。

### 27.4 公平性

审计子流和检查点不能饿死：

- terminal-data；
- terminal-control；
- file-transfer；
- 会话关闭。

终端和关闭控制优先于非紧急检查点发送；检查点延迟达到上限时必须施加背压，不能静默跳过。

---

## 28. 加密和 relay 边界

端到端连接仍由现有安全协议保护。

审计子流：

- 使用同一已认证、端到端加密连接；
- 不建立独立明文连接；
- 不向 relay 暴露审计内容；
- 不向 relay 上传本地审计文件；
- 不要求 relay 保存检查点。

本地 `.yonaudit` 由操作系统文件权限保护。0.1.4 不增加独立的审计文件内容加密和密钥导出系统。

这是有意边界：

- 避免引入复杂的密钥托管和恢复流程；
- 避免审计文件只能在原机器解密；
- 保持离线双文件验证和归档可行。

未来可以在不重解释本版本完整性契约的前提下增加外部加密封装。

---

## 29. 配置原则

0.1.4 不增加审计配置。

没有：

- `audit_enabled`；
- `audit_optional`；
- 审计目录配置；
- 审计大小上限；
- 审计保留天数；
- 审计加密开关；
- 审计采样；
- 输入明文记录；
- relay 审计地址；
- 中心审计服务地址；
- 审计失败继续运行；
- 快捷键；
- 用户选择是否记录。

固定行为用于保证：

- 每次会话语义一致；
- 企业合规声明真实；
- 不存在未审计旁路；
- 测试范围可穷尽。

---

## 30. 失败分类

至少包括：

- `AuditIdentityMissing`
- `AuditIdentityInvalid`
- `AuditIdentityPermissions`
- `AuditLedgerInvalid`
- `AuditLedgerConflict`
- `AuditDirectoryUnavailable`
- `AuditRecordCreateFailed`
- `AuditRecordWriteFailed`
- `AuditRecordSyncFailed`
- `AuditProtocolUnsupported`
- `AuditHandshakeInvalid`
- `AuditSessionBindingMismatch`
- `AuditCheckpointMismatch`
- `AuditPeerSignatureInvalid`
- `AuditFinalManifestMismatch`
- `AuditLedgerCommitFailed`
- `AuditReplayUnsafe`
- `AuditContainerInvalid`

所有错误文案固定、脱敏，不输出：

- 原始输入承诺值；
- 连接码；
- OPAQUE 状态；
- 私钥；
- 完整审计内容。

---

## 31. 威胁模型与保证

### 31.1 能发现

- 修改输出字节；
- 修改输入承诺；
- 插入事件；
- 删除事件；
- 事件调序；
- 截断单个文件；
- 拼接其他会话；
- 修改文件传输结果；
- 修改终端退出码；
- 单端伪造与对端不一致的记录；
- 替换单个会话文件；
- 删除中间会话但保留后续账本；
- 审计协议运行时出现双端分歧。

### 31.2 不能绝对防止

#### 两端全部记录和账本被整体删除

没有第三方见证时，无法证明已被两端整体删除的会话曾经存在。

#### 双端或单端历史整体回滚

没有外部账本根、可信单调计数器或第三方见证时，如果攻击者把某端的身份、账本和记录集合一起恢复到一个更早但当时合法的快照，本端无法仅凭该快照发现后续历史曾经存在。双端同时回滚具有同样边界。

#### 两端实时同时失陷

攻击者若在会话进行期间同时完全控制：

- 两个 `yon` 进程；
- 两端审计私钥；
- 两端文件系统；
- 两端账本状态；

则纯端点系统无法提供独立第三方真实性保证。

#### 双端身份和全部历史被同时重置

如果两端持久审计身份、账本、记录和所有外部副本一起被替换，外部验证者没有可信指纹锚点时只能判断新文件自洽，不能证明它们属于原端点。

#### 可信时间

本地墙钟由操作系统提供，不是可信时间戳。

### 31.3 0.1.4 的准确保证

> 对于提供出来的一对完整审计文件，0.1.4 可以检查双方嵌入身份签名、会话签名、共享事实链、本地记录链和 LocalRecordSeal，并发现内容修改、事件删除、插入、调序、截断、单端替换和双端不一致。

> 在没有可信身份或账本锚点时，该结果只能证明所提供的两份文件形成一套密码学自洽的记录，不能排除攻击者事后整体生成一对新文件和新身份。

> 至少一端持久审计身份和账本连续性仍可信时，才可以进一步认证该会话属于该已知端点的连续历史。没有外部见证时，整体删除、合法快照回滚和双端同时完全失陷仍不在保证范围内。

---

## 32. 实现分层

### 32.1 `yonder-core`

新增：

- `wire::audit`；
- `AUDIT_PROTOCOL`；
- 审计容器格式；
- 审计事件类型；
- 共享链；
- 本地链；
- 检查点；
- 共同清单；
- 账本提交；
- 严格编解码；
- 签名与验证领域类型。

### 32.2 `yon`

新增独立审计模块：

```text
audit/
├── identity
├── ledger
├── session
├── writer
├── observer
├── verify
├── replay
└── vt
```

现有 controller、host、terminal 和 file-transfer 只通过窄接口提交事件。

### 32.3 `yonder-net`

复用既有应用子流。

只需要注册和打开 `/yonder/audit/1.0.0`，不包含审计业务逻辑。

### 32.4 `yon-relay`

不增加：

- 审计协议解析；
- 审计配置；
- 审计文件；
- 审计数据库；
- 审计日志；
- 审计密钥；
- 审计验证。

---

## 33. 必测矩阵

### 33.1 身份和账本

- 首次自动创建；
- 并发首次创建；
- 身份文件已存在；
- 权限错误；
- 身份缺失但存在历史；
- 账本签名错误；
- 账本前序根错误；
- 账本原子更新崩溃恢复；
- 单一恢复候选；
- 多个冲突候选；
- 审计身份持续性；
- 多个并发会话按完成顺序串行提交；
- 锁持有者崩溃后的待提交恢复；
- 并发提交不产生账本分叉。

### 33.2 审计握手

- 正常双端；
- 旧端点；
- 角色错误；
- session binding 错误；
- nonce 错误；
- 持久身份签名错误；
- 临时公钥替换；
- 格式不兼容；
- AuditReady 不一致；
- header 写入失败；
- header sync 失败。

### 33.3 输入输出一致性

- 任意读取块切分；
- 16 KiB 边界前后；
- 空输入；
- 空输出；
- 大量输出；
- 二进制和非 UTF-8 输出；
- Windows 显示转换前的原始输出；
- Windows UTF-8 替代字符转换后的 display-bytes；
- Unix 原始输出与 display-bytes 一致性；
- resize 与输出并发；
- 输入 HMAC 承诺一致；
- 输入秘密贡献不落盘；
- 错误秘密贡献承诺拒绝；
- 对短密码候选无法仅凭审计文件离线验证；
- 输出内容一致；
- 单字节修改；
- 插入、删除和调序。

### 33.4 检查点

- 时间触发；
- 大小触发；
- TerminalReady；
- 文件传输边界；
- 终端退出；
- 对端延迟；
- 重复检查点；
- 跳号；
- 根不一致；
- 签名错误；
- checkpoint 写入失败；
- session 关闭时最终检查点；
- Checkpoint 和 Ack 不产生共享链或本地链自引用。

### 33.5 文件传输

- 上传成功；
- 下载成功；
- 空文件；
- 大文件；
- 目标存在；
- 权限错误；
- 取消；
- 网络断开；
- 路径记录；
- 文件摘要；
- 不记录文件内容；
- 文件传输失败后终端仍按既有规则工作；
- 审计失败时文件传输取消。

### 33.6 失败关闭

- 审计目录只读；
- 磁盘满；
- 文件被删除；
- writer 失败；
- writer 队列背压；
- ledger 提交失败；
- 审计子流断开；
- 检查点 mismatch；
- 签名错误；
- 审计失败后无额外输入进入 PTY；
- 审计失败后无额外输出显示给主控端。

### 33.7 容器验证

- 正常完整且至少一端有可信锚点的双文件；
- 正常完整但无可信锚点的双文件；
- 单文件；
- 无 footer；
- 截断每一个字节位置；
- frame 长度溢出；
- 未知关键 tag；
- 尾随字节；
- 修改 header；
- 修改事件；
- 修改签名；
- 修改账本提交；
- 修改 LocalRecordSeal；
- footer/ledger 摘要不存在循环依赖；
- 换用另一会话 peer 文件；
- 角色相同的两个文件；
- 一致但无本地锚点；
- 伪造一整对新身份和新文件只能得到未锚定状态。

### 33.8 安全重放

- 普通 Shell；
- 全屏 TUI；
- resize；
- Unicode；
- 非 UTF-8；
- OSC 52；
- 标题修改；
- DCS/APC/PM；
- 设备查询；
- 未知转义；
- 大文件流式重放；
- `Ctrl+C` 退出；
- 被篡改文件拒绝重放；
- 未配对文件显式警告。

### 33.9 回归

- 0.1.1 终端行为；
- 0.1.2 普通与企业认证；
- 直连；
- relay；
- WSS；
- 0.1.3 上传和下载；
- `Ctrl+]` 全部语义；
- 非交互输入；
- 六个正式 target；
- Linux、Windows、macOS 两两互连。

---

## 34. 发布验收标准

0.1.4 发布前必须全部满足：

1. 用户仍只使用 `yon host` 和 `yon connect`；
2. 每次 Terminal Active 前双端审计握手完成；
3. 任一端无法建立审计记录时会话拒绝；
4. 会话中审计写入失败时立即失败关闭；
5. relay 无审计代码、存储和配置；
6. 原始输入内容、输入承诺密钥和裸输入 SHA-256 不写入审计；
7. 输入 HMAC 承诺在双端一致，审计文件不能充当离线密码字典验证器；
8. 输出内容在双端一致；
9. 终端输出可以安全重放；
10. 终端副作用控制序列不直接透传执行；
11. resize、生命周期和退出状态可验证；
12. 0.1.3 文件传输事件可验证；
13. 文件内容不进入审计；
14. 双端完整但无可信锚点的文件只能得到 `CONSISTENT_COMPLETE_UNANCHORED`；
15. 至少一端匹配受保护身份和账本时才能得到 `VERIFIED_COMPLETE`；
16. 异常中断只能认证到最后共同检查点；
17. 修改任一事件必然导致验证失败；
18. 删除、插入和调序必然导致验证失败；
19. 单端替换无法与对端记录通过验证；
20. 并发会话账本提交不会分叉；保留较新账本头时可以发现中间会话删除，整体合法快照回滚必须按威胁边界报告；
21. 审计文件和身份权限满足平台要求；
22. 正常结束完成非循环 LocalRecordSeal、文件 sync 和串行化账本原子提交；
23. verify 和 replay 流式、有界；
24. 旧端点在 Terminal Active 前明确拒绝；
25. 普通 relay 和企业 relay 不需要审计升级；
26. 0.1.1、0.1.2、0.1.3 回归测试通过；
27. 六个平台全部通过。

---

## 35. 被排除的方向

### 35.1 只在主控端录像

无法证明主控端记录与被控端实际收到和发送的事实一致。

### 35.2 只在被控端录像

无法证明操作人员实际看到的画面和被控端发送的内容一致。

### 35.3 relay 保存审计

扩大 relay 职责，暴露敏感终端内容，破坏既有密文转发边界，并引入存储、查询、权限和保留系统。

### 35.4 解析 Shell 命令

不能跨 Shell、TUI、REPL、编辑器和操作系统可靠实现，会制造错误审计事实。

### 35.5 保存全部原始键盘输入

会把密码、Token 和其他隐藏凭证永久写入审计，且无法可靠跨平台识别何时应隐藏。

### 35.6 仅哈希单端文件

只能发现本文件变化，不能证明与对端实际会话一致。

### 35.7 只使用嵌入文件的临时自签名密钥

攻击者可以整体替换两份文件并生成一套新的临时密钥与自声明持久身份。只有受保护的现有持久审计身份、账本连续性或外部信任锚点才能把记录绑定到已知历史。

### 35.8 每事件远端同步确认

会将网络往返延迟加入每次键盘和输出，破坏远程终端体验。采用周期检查点和正常结束最终共同清单。

### 35.9 每事件 `fsync`

会显著破坏交互性能。采用 append 前置、检查点持久化和正常结束完整同步。

### 35.10 中心服务

中心见证可以解决整体删除和集中留存，但超出 0.1.4 简单纯端点范围，未来必须以独立版本设计加入。

---

## 36. 全量交叉审计

本设计已在内部按“一问一答”方式逐项决定产品目标、记录对象、输入隐私、双端一致性、持久身份、会话密钥、账本、失败关闭、异常中断、文件格式、重放安全、性能、兼容和平台行为；每项结论均与 0.1.1、0.1.2、0.1.3 及此前全部 0.1.4 结论进行叠加审计。

### 36.1 哲学统一：通过

Yonder 仍然是透明、真实的跨平台远程终端。

审计：

- 不改变用户操作；
- 不解析 Shell；
- 不改变 PTY；
- 不改变终端字节；
- 不改变文件传输；
- 不改变 relay 的不可信转发定位。

它只是对既有事实增加双端旁路取证。

### 36.2 语义一致：通过

- 终端输入仍然是终端输入；
- 审计只保存使用会话私有密钥生成的输入承诺，不保存原文或可离线猜测的裸摘要，也不将输入冒充为命令；
- 终端输出仍然是终端输出；
- 文件内容仍然只属于文件传输；
- 文件审计只记录结构化元数据；
- 双端一致性比较规范事实，不比较不同本地时间；
- `VERIFIED_COMPLETE`、`CONSISTENT_COMPLETE_UNANCHORED` 与异常中断状态严格区分。

### 36.3 逻辑自洽：通过

正常闭环：

```text
OPAQUE success
→ audit identities
→ audit handshake
→ local header synced
→ Terminal Active
→ append-before-effect recording
→ periodic bilateral checkpoints
→ terminal close
→ final shared roots
→ joint manifest
→ dual session signatures
→ local record seal
→ serialized local ledger commit appended
→ complete audit file sync
→ atomic ledger state advance
→ anchored verified complete
```

异常闭环：

```text
audit failure or mismatch
→ stop new observable effects
→ terminate terminal/file activity
→ preserve valid local prefix
→ preserve last shared checkpoint
→ explicit non-complete verification state
```

### 36.4 真实有效：通过

设计使用：

- 现有已认证连接；
- 独立应用子流；
- Ed25519；
- SHA-256；
- 顺序文件写入；
- 固定大小规范块；
- 会话私有 HMAC 输入承诺；
- 非自引用检查点和本地记录封印；
- 跨进程串行化账本提交；
- 本地私有状态目录；
- 跨平台文件权限；
- 内置安全 VT 解析器。

没有依赖：

- 不存在的可靠 Shell 解析；
- 不可获得的全局可信时间；
- relay 明文；
- 中心服务；
- 逐事件网络往返；
- 无限内存；
- 事后猜测用户操作。

### 36.5 完整可靠：通过

设计完整覆盖：

- 产品边界；
- 用户体验；
- 双端记录；
- 输入隐私；
- 终端画面；
- 身份锚定；
- 临时签名；
- 本地账本；
- 协议握手；
- 事件模型；
- 规范化；
- 哈希链；
- 检查点；
- 最终共同清单；
- 持久写入；
- 失败关闭；
- 异常中断；
- 文件传输；
- CLI 验证；
- 安全重放；
- 威胁模型；
- 旧版本；
- relay；
- 六个平台；
- 测试与验收。

没有遗留会改变 0.1.4 产品形态或外部语义的待决问题。实现阶段可以决定内部 Rust 类型、文件模块拆分、经过审计的 VT 解析依赖和固定超时细节，但不得重新解释本文冻结契约。

---

## 37. 最终冻结基线

Yonder 0.1.4 正式冻结为：

> Yonder 对每一次成功建立的远程终端会话实施纯端点、双端自动、失败关闭的可验证审计。主控端和被控端分别生成本地 `.yonaudit` 文件，并通过持久审计身份、每会话临时签名密钥、共享事实哈希链、非自引用周期检查点、最终共同清单、LocalRecordSeal 和跨进程串行化本地账本证明记录完整且双端一致。原始键盘输入不写入审计；输入使用未持久化的会话私有密钥生成 HMAC 承诺，避免审计文件成为离线密码猜测验证器。完整终端输出被记录并通过内置安全终端模拟器重放，因此终端未回显的输入内容不可恢复，但其发生、长度和双端一致性仍可验证。0.1.3 文件传输的共享事实只记录双方可验证的路径字段、大小、摘要和结果，本端独有路径只进入本地链，文件内容不进入审计。审计不依赖 relay，不增加会话操作步骤，不解析 Shell，不改变 0.1.4 双端之间终端、认证、连接和文件传输的既有语义。

同时冻结以下认证边界：

> 双端完整文件匹配、双方嵌入身份签名与会话签名有效、全部共享事实链、各自本地链和 LocalRecordSeal 有效，但不存在可信身份或账本锚点时，只能输出 `CONSISTENT_COMPLETE_UNANCHORED`。至少一端同时匹配受保护的持久审计身份和账本连续性时，才输出 `VERIFIED_COMPLETE`。在无第三方见证的纯端点模型中，0.1.4 不承诺证明两端记录、身份和账本被整体删除、整体回滚或同时重置的会话曾经存在，也不承诺抵抗会话期间双端同时完全失陷。

本文可直接作为 Yonder 0.1.4 的实现、任务拆分、协议审查、安全审查、测试和发布验收基线。
